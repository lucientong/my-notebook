# AI模型训练与优化

> 如果你现在对训练、微调、RLHF、量化这些概念还停留在“知道名字，但不知道各自处在什么阶段”，建议先读开头两节，再进入 LoRA、PPO、GPTQ 这些具体技术。

---

## 📑 目录

0. [开始前先看](#开始前先看)
1. [先从训练全链路理解这篇](#1-先从训练全链路理解这篇)

### 训练基础
2. [模型训练基础](#模型训练基础)（训练流程、损失函数、优化器、学习率调度）

### 微调与对齐
3. [Fine-tuning微调](#fine-tuning微调)（全量微调、LoRA、QLoRA、Prefix Tuning）
4. [RLHF强化学习](#rlhf强化学习)（RLHF流程、PPO算法、DPO算法）

### 推理与部署
5. [模型量化](#模型量化)（PTQ、QAT、GPTQ、AWQ）
6. [模型部署优化](#模型部署优化)（推理加速、批处理优化、KV Cache、Flash Attention）

### 分布式与自查
7. [分布式训练](#分布式训练)（数据并行、模型并行、流水线并行、ZeRO优化）
8. [面试题自查](#面试题自查)

---

## 开始前先看

### 这篇解决什么问题

训练相关内容常见的问题是：

- 知道 LoRA 是微调
- 知道 RLHF 是对齐
- 知道量化能降成本
- 知道 ZeRO 是分布式优化

但这些知识点彼此断开，不知道它们分别处在模型生命周期的哪个位置。

这篇的目标是把训练相关问题串成一条完整链路：

- 模型怎么从无到有训练出来
- 为什么预训练之后还要 SFT
- 为什么 SFT 之后还要偏好对齐
- 为什么训练完之后还要量化、蒸馏、部署优化
- 为什么模型一大，就必须考虑分布式训练

训练、对齐、压缩、部署不是并列概念，而是模型生命周期里前后相接、逐层补短板的阶段。

---

## 1. 先从训练全链路理解这篇

### 1.0 一条完整主线

先看完整链路：

```text
原始数据
  ↓
预训练
  ↓        让模型学会语言和模式
基础模型
  ↓
SFT / 微调
  ↓        让模型学会按任务回答
指令模型
  ↓
RLHF / DPO
  ↓        让模型更符合人类偏好
对齐模型
  ↓
量化 / 蒸馏 / 推理优化
  ↓        让模型更便宜、更快、更好部署
生产可用模型
```

这条链路里，每个阶段都有明确的职责：

- `预训练`：解决“模型先要会语言”。
- `SFT`：解决“模型要能按要求完成任务”。
- `RLHF / DPO`：解决“模型回答要更符合人类偏好”。
- `量化 / 蒸馏`：解决“模型太贵太慢，上不了生产”。
- `分布式训练`：因为模型和数据大到单机撑不住。

### 1.0.1 先区分 4 类常见问题

| 现象 | 更可能的问题 | 应该优先看什么 |
|------|-------------|---------------|
| 模型根本不会这类任务 | 预训练或 SFT 不足 | 训练基础 / Fine-tuning |
| 模型会做，但回答不符合偏好 | 对齐不足 | RLHF / DPO |
| 模型效果够，但太慢太贵 | 部署成本高 | 量化 / 推理优化 |
| 模型太大，单机训不动 | 资源瓶颈 | 分布式训练 |

### 1.0.2 阅读时先抓 5 个关键词

- `pretrain`：学语言模式
- `fine-tune`：学任务格式
- `align`：学偏好和安全边界
- `compress`：降低成本
- `parallel`：把大模型训练真正跑起来

如果先把这 5 个词放到正确位置，LoRA、QLoRA、PPO、GPTQ、ZeRO 就更容易理解。

## 模型训练基础

### 训练流程

**标准训练流程**：

```python
# 1. 准备数据
train_dataloader = DataLoader(train_dataset, batch_size=32, shuffle=True)

# 2. 初始化模型
model = GPT2LMHeadModel.from_pretrained('gpt2')
model.to(device)

# 3. 配置优化器
optimizer = AdamW(model.parameters(), lr=5e-5, weight_decay=0.01)
scheduler = get_linear_schedule_with_warmup(
    optimizer, 
    num_warmup_steps=100, 
    num_training_steps=len(train_dataloader) * epochs
)

# 4. 训练循环
for epoch in range(epochs):
    model.train()
    for batch in train_dataloader:
        # Forward pass
        outputs = model(**batch)
        loss = outputs.loss
        
        # Backward pass
        loss.backward()
        
        # 梯度裁剪（防止梯度爆炸）
        torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)
        
        # 参数更新
        optimizer.step()
        scheduler.step()
        optimizer.zero_grad()
    
    # 验证
    model.eval()
    with torch.no_grad():
        for batch in val_dataloader:
            outputs = model(**batch)
            # 计算验证指标
```

---

### 损失函数

**语言模型的损失函数**：

```python
# Cross-Entropy Loss（交叉熵损失）
# 目标：预测下一个token

# 示例：预测 "The cat sits on the mat"
# 输入：["The", "cat", "sits", "on", "the"]
# 标签：["cat", "sits", "on", "the", "mat"]

def compute_loss(logits, labels):
    """
    logits: (batch_size, seq_len, vocab_size)
    labels: (batch_size, seq_len)
    """
    # Shift：预测下一个token
    shift_logits = logits[..., :-1, :].contiguous()  # (batch, seq-1, vocab)
    shift_labels = labels[..., 1:].contiguous()      # (batch, seq-1)
    
    # 计算交叉熵
    loss_fct = nn.CrossEntropyLoss(ignore_index=-100)
    loss = loss_fct(
        shift_logits.view(-1, shift_logits.size(-1)),  # (batch*(seq-1), vocab)
        shift_labels.view(-1)                           # (batch*(seq-1))
    )
    return loss

# 困惑度（Perplexity）：评估语言模型性能
perplexity = torch.exp(loss)
# 困惑度越低，模型越好（理想值接近1）
```

---

### 优化器

**常用优化器对比**：

| 优化器 | 特点 | 适用场景 |
|--------|------|----------|
| **SGD** | 简单、内存占用小 | 小模型 |
| **Adam** | 自适应学习率、常用 | 大部分场景 |
| **AdamW** | Adam + 权重衰减 | Transformer模型（推荐） |
| **Adafactor** | 内存高效（不存储一阶动量） | 超大模型 |

**AdamW实现**：

```python
class AdamW(Optimizer):
    def __init__(self, params, lr=1e-3, betas=(0.9, 0.999), eps=1e-8, weight_decay=0.01):
        self.lr = lr
        self.beta1, self.beta2 = betas
        self.eps = eps
        self.weight_decay = weight_decay
        
        # 一阶动量（梯度的指数移动平均）
        self.m = [torch.zeros_like(p) for p in params]
        # 二阶动量（梯度平方的指数移动平均）
        self.v = [torch.zeros_like(p) for p in params]
        self.t = 0
    
    def step(self, grads):
        self.t += 1
        for i, (param, grad) in enumerate(zip(self.params, grads)):
            # 更新一阶动量
            self.m[i] = self.beta1 * self.m[i] + (1 - self.beta1) * grad
            # 更新二阶动量
            self.v[i] = self.beta2 * self.v[i] + (1 - self.beta2) * grad ** 2
            
            # 偏差修正
            m_hat = self.m[i] / (1 - self.beta1 ** self.t)
            v_hat = self.v[i] / (1 - self.beta2 ** self.t)
            
            # 参数更新
            param -= self.lr * m_hat / (torch.sqrt(v_hat) + self.eps)
            
            # 权重衰减（与L2正则化不同，直接衰减参数）
            param -= self.lr * self.weight_decay * param
```

---

### 学习率调度

**常用调度策略**：

```python
# 1. 线性预热 + 线性衰减
from transformers import get_linear_schedule_with_warmup

scheduler = get_linear_schedule_with_warmup(
    optimizer,
    num_warmup_steps=1000,   # 预热步数
    num_training_steps=10000  # 总训练步数
)

# 学习率变化：
# 0 → 1000步：线性增长到 lr
# 1000 → 10000步：线性衰减到 0

# 2. 余弦退火
from torch.optim.lr_scheduler import CosineAnnealingLR

scheduler = CosineAnnealingLR(optimizer, T_max=10000, eta_min=1e-6)

# 学习率按余弦函数衰减

# 3. 阶梯式衰减
from torch.optim.lr_scheduler import StepLR

scheduler = StepLR(optimizer, step_size=1000, gamma=0.1)

# 每1000步，学习率乘以0.1
```

**为什么需要预热（Warmup）？**

```
问题：训练初期，模型参数随机初始化，梯度可能很大

解决：
1. 学习率从小开始（如1e-7）
2. 逐步增大到目标学习率（如5e-5）
3. 避免训练初期参数更新过大导致不稳定

示例：
Step 0:    lr = 1e-7
Step 500:  lr = 2.5e-5
Step 1000: lr = 5e-5  ← 预热结束
```

---

## Fine-tuning微调

### 全量微调

**定义**：更新模型的所有参数。

```python
# 加载预训练模型
model = AutoModelForCausalLM.from_pretrained("gpt2")

# 准备数据
train_dataset = TextDataset(tokenizer, "train.txt")
data_collator = DataCollatorForLanguageModeling(tokenizer, mlm=False)

# 训练参数
training_args = TrainingArguments(
    output_dir="./output",
    num_train_epochs=3,
    per_device_train_batch_size=4,
    learning_rate=5e-5,
    save_steps=1000
)

# 训练
trainer = Trainer(
    model=model,
    args=training_args,
    train_dataset=train_dataset,
    data_collator=data_collator
)
trainer.train()
```

**问题**：
- 显存占用大（需要存储所有参数的梯度）
- 训练时间长
- 容易过拟合（小数据集）

---

### LoRA

**定义**：Low-Rank Adaptation，低秩适配。

**核心思想**：
- 冻结预训练模型参数
- 只训练少量低秩矩阵

```
原始矩阵更新：
W' = W + ΔW
ΔW ∈ R^(d×d)  # 需要训练 d×d 个参数

LoRA：
W' = W + BA
B ∈ R^(d×r), A ∈ R^(r×d)  # 只需要训练 2×d×r 个参数

当 r << d 时，参数量大幅减少
示例：d=4096, r=8
原始：4096×4096 = 16,777,216 参数
LoRA：2×4096×8 = 65,536 参数（减少256倍）
```

**实现**：

```python
import torch.nn as nn

class LoRALayer(nn.Module):
    def __init__(self, in_features, out_features, rank=8, alpha=16):
        super().__init__()
        self.rank = rank
        self.alpha = alpha
        
        # 低秩矩阵
        self.lora_A = nn.Parameter(torch.randn(in_features, rank) * 0.01)
        self.lora_B = nn.Parameter(torch.zeros(rank, out_features))
        
        # 缩放因子
        self.scaling = alpha / rank
    
    def forward(self, x):
        # LoRA输出
        lora_output = (x @ self.lora_A @ self.lora_B) * self.scaling
        return lora_output

# 使用LoRA微调
from peft import get_peft_model, LoraConfig, TaskType

peft_config = LoraConfig(
    task_type=TaskType.CAUSAL_LM,
    r=8,                    # 秩
    lora_alpha=16,          # 缩放因子
    lora_dropout=0.1,       # Dropout
    target_modules=["q_proj", "v_proj"]  # 应用LoRA的模块
)

model = AutoModelForCausalLM.from_pretrained("gpt2")
model = get_peft_model(model, peft_config)

# 只有LoRA参数会被训练
model.print_trainable_parameters()
# 输出：trainable params: 294,912 || all params: 124,439,808 || trainable%: 0.24
```

**LoRA优势**：
- ✅ 参数量减少100-1000倍
- ✅ 显存占用减少
- ✅ 训练速度快
- ✅ 可以保存多个LoRA版本（不同任务）

---

### QLoRA

**定义**：Quantized LoRA，量化+LoRA。

**创新**：
1. **4-bit量化**：将模型权重量化为4-bit（FP16→INT4）
2. **NF4（Normal Float 4）**：专为正态分布权重设计的4-bit格式
3. **双量化**：量化缩放因子也量化

```python
from transformers import BitsAndBytesConfig

# 配置4-bit量化
bnb_config = BitsAndBytesConfig(
    load_in_4bit=True,
    bnb_4bit_quant_type="nf4",       # 使用NF4
    bnb_4bit_use_double_quant=True,  # 双量化
    bnb_4bit_compute_dtype=torch.bfloat16  # 计算时用BF16
)

# 加载量化模型
model = AutoModelForCausalLM.from_pretrained(
    "meta-llama/Llama-2-7b-hf",
    quantization_config=bnb_config,
    device_map="auto"  # 自动分配到GPU
)

# 应用LoRA
from peft import prepare_model_for_kbit_training, LoraConfig, get_peft_model

model = prepare_model_for_kbit_training(model)
peft_config = LoraConfig(r=16, lora_alpha=32, target_modules=["q_proj", "v_proj"])
model = get_peft_model(model, peft_config)

# 训练（显存占用：7B模型只需要6GB显存）
trainer.train()
```

**QLoRA效果**：
- **Llama-2-7B**：
  - FP16全量微调：28GB显存
  - LoRA：14GB显存
  - QLoRA：**6GB显存**（减少4.6倍）

---

### Prefix Tuning

**定义**：在输入前添加可训练的前缀。

```python
# 原始输入
input_ids: [101, 2023, 2003, ...]  # "This is ..."

# Prefix Tuning
prefix: [trainable_1, trainable_2, ..., trainable_n]  # n个可训练token
input_ids: [prefix, 101, 2023, 2003, ...]

# 只训练prefix参数，模型参数冻结
```

**实现**：

```python
class PrefixEncoder(nn.Module):
    def __init__(self, prefix_length, hidden_size):
        super().__init__()
        self.prefix_length = prefix_length
        
        # 可训练的prefix embedding
        self.prefix_embedding = nn.Parameter(
            torch.randn(prefix_length, hidden_size)
        )
    
    def forward(self, batch_size):
        # 扩展到batch维度
        prefix = self.prefix_embedding.unsqueeze(0).expand(batch_size, -1, -1)
        return prefix

# 使用
prefix_encoder = PrefixEncoder(prefix_length=10, hidden_size=768)
prefix = prefix_encoder(batch_size=32)  # (32, 10, 768)

# 拼接到输入
input_embeds = model.get_input_embeddings()(input_ids)  # (32, seq_len, 768)
input_embeds = torch.cat([prefix, input_embeds], dim=1)  # (32, 10+seq_len, 768)

# 前向传播
outputs = model(inputs_embeds=input_embeds)
```

---

## RLHF强化学习

### RLHF流程

**定义**：Reinforcement Learning from Human Feedback，从人类反馈中强化学习。

**三阶段流程**：

```
Stage 1: 预训练（Pretraining）
- 大规模无标注数据训练
- 目标：语言建模能力

Stage 2: 监督微调（SFT, Supervised Fine-Tuning）
- 高质量问答对训练
- 目标：回答问题的能力

Stage 3: 强化学习（RL）
- 人类偏好数据训练奖励模型
- 用奖励模型指导策略优化
- 目标：生成符合人类偏好的回答
```

---

### PPO算法

**定义**：Proximal Policy Optimization，近端策略优化。

**核心思想**：
- 限制策略更新幅度（防止训练崩溃）
- 使用重要性采样（利用旧策略的数据）

**算法流程**：

```python
# 1. 采样：使用当前策略生成回答
prompts = ["What is AI?", "Explain quantum computing", ...]
responses = policy_model.generate(prompts)

# 2. 计算奖励：使用奖励模型评分
rewards = reward_model(prompts, responses)

# 3. 计算优势函数（Advantage）
advantages = compute_advantages(rewards, values)

# 4. PPO损失
def ppo_loss(old_log_probs, new_log_probs, advantages, clip_epsilon=0.2):
    # 重要性采样比率
    ratio = torch.exp(new_log_probs - old_log_probs)
    
    # 裁剪目标
    clipped_ratio = torch.clamp(ratio, 1 - clip_epsilon, 1 + clip_epsilon)
    
    # PPO损失（取最小值，保守更新）
    loss = -torch.min(
        ratio * advantages,
        clipped_ratio * advantages
    ).mean()
    
    return loss

# 5. 更新策略
for epoch in range(ppo_epochs):
    new_log_probs = policy_model.get_log_probs(prompts, responses)
    loss = ppo_loss(old_log_probs, new_log_probs, advantages)
    loss.backward()
    optimizer.step()
```

**为什么要裁剪比率？**

```
问题：策略更新太大 → 训练不稳定 → 性能崩溃

解决：限制比率在 [1-ε, 1+ε] 范围内（如 [0.8, 1.2]）
- 如果新策略比旧策略好太多 → 裁剪到1.2
- 如果新策略比旧策略差太多 → 裁剪到0.8
- 确保平滑更新
```

---

### DPO算法

**定义**：Direct Preference Optimization，直接偏好优化。

**创新**：
- 不需要训练奖励模型
- 直接从偏好数据优化策略

**训练数据格式**：
```python
# 偏好数据：(prompt, chosen_response, rejected_response)
data = [
    {
        "prompt": "What is AI?",
        "chosen": "AI is artificial intelligence, which...",    # 人类偏好
        "rejected": "AI is a robot that can think."            # 不偏好
    },
    ...
]
```

**DPO损失**：

```python
def dpo_loss(policy_model, ref_model, prompt, chosen, rejected, beta=0.1):
    # 策略模型的log概率
    policy_chosen_logp = policy_model.get_log_prob(prompt, chosen)
    policy_rejected_logp = policy_model.get_log_prob(prompt, rejected)
    
    # 参考模型的log概率（冻结）
    ref_chosen_logp = ref_model.get_log_prob(prompt, chosen)
    ref_rejected_logp = ref_model.get_log_prob(prompt, rejected)
    
    # DPO损失
    loss = -torch.log(torch.sigmoid(
        beta * (policy_chosen_logp - ref_chosen_logp) -
        beta * (policy_rejected_logp - ref_rejected_logp)
    )).mean()
    
    return loss

# 目标：
# - 增大 chosen 的概率
# - 减小 rejected 的概率
# - 不偏离参考模型太远（防止过拟合）
```

**DPO vs PPO**：

| 特性 | PPO | DPO |
|------|-----|-----|
| **需要奖励模型** | ✅ 需要 | ❌ 不需要 |
| **训练复杂度** | 高（两个模型） | 低（一个模型） |
| **稳定性** | 较不稳定 | 较稳定 |
| **效果** | 稍好 | 接近 |

---

## 模型量化

### 量化原理

**定义**：将高精度权重（FP32/FP16）转换为低精度（INT8/INT4）。

**动机**：
- 减少模型大小（4倍~16倍）
- 降低推理时间（2倍~4倍）
- 降低显存占用

**量化公式**：

```
量化：
x_quantized = round((x - zero_point) / scale)

反量化：
x_dequantized = x_quantized * scale + zero_point

参数计算：
scale = (x_max - x_min) / (q_max - q_min)
zero_point = q_min - x_min / scale

示例：FP32 → INT8
x_min = -10.0, x_max = 10.0
q_min = -128, q_max = 127

scale = (10 - (-10)) / (127 - (-128)) = 20 / 255 ≈ 0.0784
zero_point = -128 - (-10) / 0.0784 ≈ 0

x = 5.0:
  量化：round((5.0 - 0) / 0.0784) = 64
  反量化：64 * 0.0784 + 0 = 5.018  # 略有误差
```

---

### PTQ训练后量化

**定义**：Post-Training Quantization，模型训练完成后再量化。

**步骤**：
1. 收集激活值分布（Calibration）
2. 计算量化参数（scale, zero_point）
3. 量化权重和激活值

```python
import torch.quantization as quantization

# 1. 加载模型
model = MyModel()
model.eval()

# 2. 配置量化
model.qconfig = quantization.get_default_qconfig('fbgemm')  # x86 CPU
quantization.prepare(model, inplace=True)

# 3. 校准（Calibration）：收集激活值分布
with torch.no_grad():
    for batch in calibration_dataloader:
        model(batch)

# 4. 转换为量化模型
quantization.convert(model, inplace=True)

# 5. 推理
output = model(input)

# 模型大小对比
# FP32: 100MB
# INT8: 25MB（减少4倍）
```

**优点**：
- ✅ 简单（不需要重新训练）
- ✅ 快速

**缺点**：
- ⚠️ 精度损失（1-5%）

---

### QAT量化感知训练

**定义**：Quantization-Aware Training，训练时模拟量化。

**原理**：
- 前向传播：量化→反量化（模拟量化误差）
- 反向传播：FP32梯度（正常更新）

```python
import torch.quantization as quantization

# 1. 配置量化
model.qconfig = quantization.get_default_qat_qconfig('fbgemm')
quantization.prepare_qat(model, inplace=True)

# 2. 训练（模拟量化）
model.train()
for epoch in range(epochs):
    for batch in train_dataloader:
        outputs = model(batch)  # 内部进行 量化→反量化
        loss = criterion(outputs, labels)
        loss.backward()
        optimizer.step()

# 3. 转换为真正的量化模型
model.eval()
quantization.convert(model, inplace=True)

# 4. 推理
output = model(input)
```

**优点**：
- ✅ 精度损失小（< 1%）
- ✅ 适合对精度敏感的任务

**缺点**：
- ⚠️ 需要重新训练（时间长）

---

### GPTQ

**定义**：Generative Pre-trained Transformer Quantization，针对大模型的量化方法。

**核心思想**：
- 逐层量化（减少累积误差）
- 使用Hessian矩阵指导量化（重要权重用高精度）

```python
from auto_gptq import AutoGPTQForCausalLM, BaseQuantizeConfig

# 配置
quantize_config = BaseQuantizeConfig(
    bits=4,                    # 4-bit量化
    group_size=128,            # 分组大小
    desc_act=False             # 是否量化激活值
)

# 加载模型
model = AutoGPTQForCausalLM.from_pretrained(
    "meta-llama/Llama-2-7b-hf",
    quantize_config
)

# 量化
model.quantize(calibration_dataloader)

# 保存
model.save_quantized("./llama-2-7b-gptq")

# 加载量化模型
model = AutoGPTQForCausalLM.from_quantized("./llama-2-7b-gptq")
```

**效果**：
- **Llama-2-7B**：
  - FP16: 14GB
  - GPTQ (4-bit): **3.5GB**（减少4倍）
  - 困惑度损失：< 2%

---

### AWQ

**定义**：Activation-aware Weight Quantization，激活感知的权重量化。

**核心思想**：
- 分析激活值的重要性
- 重要通道的权重使用高精度
- 不重要通道的权重激进量化

```python
from awq import AutoAWQForCausalLM

# 加载模型
model = AutoAWQForCausalLM.from_pretrained("meta-llama/Llama-2-7b-hf")

# 量化
model.quantize(
    calibration_dataloader,
    quant_config={
        "zero_point": True,
        "q_group_size": 128,
        "w_bit": 4
    }
)

# 保存
model.save_quantized("./llama-2-7b-awq")

# 加载
model = AutoAWQForCausalLM.from_quantized("./llama-2-7b-awq")
```

**AWQ vs GPTQ**：

| 特性 | GPTQ | AWQ |
|------|------|-----|
| **量化速度** | 慢（需要Hessian计算） | 快 |
| **推理速度** | 快 | 更快 |
| **精度** | 高 | 略高于GPTQ |

---

## 模型部署优化

### 推理加速

**常用加速技术**：

**1. vLLM**：
```python
from vllm import LLM, SamplingParams

# 初始化
llm = LLM(model="meta-llama/Llama-2-7b-hf", tensor_parallel_size=2)

# 推理
prompts = ["Hello, how are you?", "What is AI?"]
sampling_params = SamplingParams(temperature=0.8, top_p=0.95, max_tokens=100)
outputs = llm.generate(prompts, sampling_params)

# 特性：
# - PagedAttention：动态KV Cache管理
# - Continuous Batching：动态批处理
# - 吞吐量提升：24倍（相比HuggingFace Transformers）
```

**2. TensorRT-LLM**：
```python
# NVIDIA的LLM推理引擎
# 特性：
# - INT8/FP16/BF16优化
# - Flash Attention
# - 多GPU推理
# - 延迟降低：2-4倍
```

**3. Text Generation Inference (TGI)**：
```bash
# HuggingFace的推理服务
docker run -p 8080:80 \
  --gpus all \
  --shm-size 1g \
  ghcr.io/huggingface/text-generation-inference:latest \
  --model-id meta-llama/Llama-2-7b-hf \
  --quantize bitsandbytes

# 特性：
# - Continuous Batching
# - Token Streaming
# - 量化支持
# - 生产级别
```

---

### 批处理优化

**问题**：不同长度的序列如何批处理？

**Padding策略**：
```python
# 输入
texts = [
    "Hello",           # 1个token
    "How are you?",    # 3个token
    "What is AI?"      # 3个token
]

# Padding到最大长度
# [PAD] = 0
input_ids = [
    [101, 0, 0],       # "Hello" + 2个PAD
    [101, 102, 103],   # "How are you?"
    [104, 105, 106]    # "What is AI?"
]

# 注意力掩码（忽略PAD）
attention_mask = [
    [1, 0, 0],
    [1, 1, 1],
    [1, 1, 1]
]
```

**问题**：浪费计算（PAD也参与计算）

**Flash Attention解决方案**：跳过PAD的计算

---

### KV Cache

**问题**：自回归生成时，每次都要重新计算之前token的K和V。

```python
# 生成 "Hello world!"
# Step 1: 输入 "Hello"
#   计算 K_hello, V_hello
#   输出 "world"

# Step 2: 输入 "Hello world"
#   重新计算 K_hello, V_hello  ← 浪费！
#   计算 K_world, V_world
#   输出 "!"

# Step 3: 输入 "Hello world !"
#   重新计算 K_hello, V_hello, K_world, V_world  ← 浪费！
#   计算 K_!
#   输出 <EOS>
```

**KV Cache优化**：
```python
# 缓存已计算的K和V
cache = {
    "past_key_values": []  # 存储每层的 (K, V)
}

# Step 1: 输入 "Hello"
outputs = model(input_ids=[101], past_key_values=None)
cache["past_key_values"] = outputs.past_key_values  # 保存 K_hello, V_hello

# Step 2: 输入 "world" （只计算新token）
outputs = model(input_ids=[102], past_key_values=cache["past_key_values"])
cache["past_key_values"] = outputs.past_key_values  # 更新缓存

# Step 3: 输入 "!"
outputs = model(input_ids=[103], past_key_values=cache["past_key_values"])

# 加速：3倍（避免重复计算）
```

**KV Cache的显存占用**：
```
每层的KV Cache大小 = 2 * batch_size * seq_len * hidden_size * 2字节（FP16）

示例：Llama-2-7B（32层）
- batch_size=1, seq_len=2048, hidden_size=4096
- KV Cache = 2 * 1 * 2048 * 4096 * 32 * 2 = 1GB

生成长文本时，KV Cache可能占用大量显存！
```

---

### Flash Attention

**问题**：标准Attention的复杂度是O(N²)，显存占用大。

**Flash Attention优化**：
- 分块计算（Tiled Attention）
- 减少HBM（高带宽内存）访问
- 使用SRAM（片上内存）

```python
# 标准Attention
def standard_attention(Q, K, V):
    # Q, K, V: (batch, num_heads, seq_len, head_dim)
    scores = Q @ K.transpose(-2, -1) / sqrt(head_dim)  # (batch, num_heads, seq_len, seq_len)
    attn = softmax(scores, dim=-1)
    output = attn @ V
    return output

# 问题：scores矩阵占用 O(N²) 显存

# Flash Attention
def flash_attention(Q, K, V, block_size=64):
    # 分块计算，每次只加载block_size的数据到SRAM
    # 显存占用：O(N)
    # 速度：2-4倍
    pass

# 使用
from flash_attn import flash_attn_func

output = flash_attn_func(Q, K, V, causal=True)  # causal=True表示因果注意力
```

**效果**：
- 速度：2-4倍
- 显存：减少10倍（长序列）

---

## 分布式训练

### 数据并行

**定义**：每个GPU有完整模型副本，处理不同数据。

```python
import torch.distributed as dist
from torch.nn.parallel import DistributedDataParallel as DDP

# 初始化分布式环境
dist.init_process_group(backend="nccl")

# 创建模型
model = MyModel().to(device)
model = DDP(model, device_ids=[local_rank])

# 训练
for batch in train_dataloader:
    outputs = model(batch)
    loss = outputs.loss
    loss.backward()
    
    # 自动同步梯度（all-reduce）
    optimizer.step()
    optimizer.zero_grad()
```

**通信模式**：
```
All-Reduce（梯度同步）：
GPU 0: grad_0
GPU 1: grad_1
GPU 2: grad_2
GPU 3: grad_3
    ↓
每个GPU：(grad_0 + grad_1 + grad_2 + grad_3) / 4
```

---

### 模型并行

**定义**：模型分割到多个GPU，每个GPU处理模型的一部分。

```python
# 示例：将模型分割到2个GPU
class ModelParallel(nn.Module):
    def __init__(self):
        super().__init__()
        self.layer1 = nn.Linear(1000, 1000).to('cuda:0')
        self.layer2 = nn.Linear(1000, 1000).to('cuda:0')
        self.layer3 = nn.Linear(1000, 1000).to('cuda:1')  # 放到GPU 1
        self.layer4 = nn.Linear(1000, 10).to('cuda:1')
    
    def forward(self, x):
        x = x.to('cuda:0')
        x = self.layer1(x)
        x = self.layer2(x)
        
        x = x.to('cuda:1')  # 数据移动到GPU 1
        x = self.layer3(x)
        x = self.layer4(x)
        return x
```

**问题**：GPU利用率低（前一层计算时，后一层空闲）

---

### 流水线并行

**定义**：将batch分成多个micro-batch，流水线执行。

```
流水线并行（Pipeline Parallelism）：
Time → 
GPU 0: [Micro-batch 1] [Micro-batch 2] [Micro-batch 3]
GPU 1:                 [Micro-batch 1] [Micro-batch 2] [Micro-batch 3]

提高GPU利用率！
```

```python
from torch.distributed.pipeline.sync import Pipe

# 将模型分成多个阶段
model = nn.Sequential(
    nn.Linear(1000, 1000),  # Stage 0
    nn.ReLU(),
    nn.Linear(1000, 1000),  # Stage 1
    nn.ReLU(),
    nn.Linear(1000, 10)     # Stage 2
)

# 流水线并行
model = Pipe(model, chunks=8)  # 将batch分成8个micro-batch

# 训练
outputs = model(inputs)
```

---

### ZeRO优化

**定义**：Zero Redundancy Optimizer，零冗余优化器。

**核心思想**：分割优化器状态、梯度、参数到多个GPU。

**三个阶段**：

```
ZeRO Stage 1：分割优化器状态
- 每个GPU只保存部分优化器状态（m, v）
- 显存减少：4倍（对于Adam）

ZeRO Stage 2：分割梯度
- 每个GPU只保存部分梯度
- 显存减少：8倍

ZeRO Stage 3：分割参数
- 每个GPU只保存部分参数
- 前向传播时，动态收集参数
- 反向传播后，立即释放参数
- 显存减少：N倍（N=GPU数量）

示例：8个GPU训练GPT-3（175B参数）
- 无ZeRO：每个GPU需要 700GB 显存（训练不了）
- ZeRO Stage 3：每个GPU需要 88GB 显存（可训练）
```

```python
from deepspeed import zero

# 配置
ds_config = {
    "zero_optimization": {
        "stage": 3,                      # ZeRO Stage 3
        "offload_optimizer": {
            "device": "cpu",             # 优化器状态卸载到CPU
            "pin_memory": True
        },
        "offload_param": {
            "device": "cpu",             # 参数卸载到CPU
            "pin_memory": True
        }
    }
}

# 初始化
model_engine, optimizer, _, _ = deepspeed.initialize(
    model=model,
    optimizer=optimizer,
    config=ds_config
)

# 训练
for batch in train_dataloader:
    loss = model_engine(batch)
    model_engine.backward(loss)
    model_engine.step()
```

---

## 面试题自查

### Q1: LoRA为什么能减少参数量？原理是什么？

**答案**：
LoRA（Low-Rank Adaptation）利用低秩分解减少可训练参数。

**原理**：
- 原始更新：ΔW ∈ R^(d×d)，参数量 d²
- LoRA：ΔW = BA，B ∈ R^(d×r)，A ∈ R^(r×d)，参数量 2dr
- 当 r << d 时，2dr << d²

**示例**：d=4096, r=8
- 原始：4096² = 16M 参数
- LoRA：2×4096×8 = 64K 参数（减少256倍）

**为什么有效**：
- 任务特定的权重更新本身就是低秩的
- 大模型的表示空间冗余

---

### Q2: QLoRA相比LoRA有什么改进？

**答案**：
QLoRA结合了量化和LoRA，进一步降低显存需求。

**三个关键创新**：
1. **4-bit NF4量化**：将预训练权重量化为4-bit（FP16→INT4）
2. **双量化**：量化的缩放因子也量化（再省显存）
3. **分页优化器**：优化器状态按需分页到CPU

**效果对比（Llama-2-7B）**：
| 方法 | 显存 |
|------|------|
| FP16全量微调 | 28GB |
| LoRA | 14GB |
| QLoRA | **6GB** |

---

### Q3: RLHF的三个阶段分别是什么？

**答案**：

| 阶段 | 目标 | 数据 |
|------|------|------|
| **Stage 1: 预训练** | 语言建模能力 | 大规模无标注文本（TB级） |
| **Stage 2: SFT** | 指令遵循能力 | 高质量问答对（10K-100K） |
| **Stage 3: RLHF** | 对齐人类偏好 | 偏好对比数据 + PPO优化 |

**Stage 3详细流程**：
1. 收集人类偏好数据（A vs B，哪个更好）
2. 训练奖励模型（Reward Model）
3. 用PPO算法优化策略模型

---

### Q4: DPO相比PPO有什么优势？

**答案**：

| 维度 | PPO | DPO |
|------|-----|-----|
| 奖励模型 | 需要单独训练 | 不需要 |
| 训练复杂度 | 高（4个模型同时） | 低（2个模型） |
| 稳定性 | 需要仔细调参 | 稳定，类似分类任务 |
| 训练阶段 | 3阶段 | 2阶段 |
| 效果 | 天花板略高 | 接近PPO |

**DPO核心思想**：
直接从偏好数据优化策略，无需强化学习循环。损失函数等价于隐式的奖励学习。

---

### Q5: 量化为什么能加速推理？

**答案**：

1. **减少内存带宽**：
   - INT8比FP32小4倍，数据加载更快
   - 现代GPU的计算瓶颈往往是内存带宽

2. **硬件加速指令**：
   - Intel VNNI（INT8加速）
   - NVIDIA Tensor Core（INT8/INT4）
   - 专用指令比通用浮点快2-4倍

3. **缓存友好**：
   - 模型更小，更多参数放入L2/L3缓存
   - 减少主存访问

**加速效果**：INT8通常比FP32快2-4倍

---

### Q6: PTQ和QAT有什么区别？如何选择？

**答案**：

| 特性 | PTQ（训练后量化） | QAT（量化感知训练） |
|------|------------------|---------------------|
| 训练需求 | 不需要训练 | 需要重新训练 |
| 时间成本 | 分钟级 | 小时/天级 |
| 精度损失 | 1-5% | < 1% |
| 校准数据 | 少量（100-1000条） | 完整训练集 |

**选择建议**：
- 对精度不敏感 → PTQ（快速上线）
- 对精度敏感 → QAT（质量优先）
- 超大模型 → PTQ（重训练成本太高）

---

### Q7: KV Cache如何节省计算？内存占用如何估算？

**答案**：

**计算节省原理**：
```
自回归生成：每个新token需要attend到所有历史token
没有KV Cache：每次重新计算所有历史的K、V
有KV Cache：缓存历史K、V，只计算新token的K、V
加速：约3倍
```

**内存占用估算**：
```
KV Cache = 2 × num_layers × batch_size × seq_len × num_heads × head_dim × dtype_size

示例（Llama-2-7B，seq=4096，batch=1，FP16）：
= 2 × 32 × 1 × 4096 × 32 × 128 × 2 bytes
= 2GB
```

**优化方案**：
- PagedAttention：分页管理，减少碎片
- GQA：分组Key-Value，减少KV数量
- 量化：KV Cache做INT8

---

### Q8: Flash Attention为什么能同时加速和省显存？

**答案**：

**标准Attention问题**：
- 显存占用 O(N²)：需要存储完整的attention矩阵
- HBM带宽瓶颈：大量数据在GPU HBM中移动

**Flash Attention解决方案**：
1. **分块计算（Tiling）**：将QKV分成小块，逐块计算
2. **使用SRAM**：小块数据放在片上SRAM（比HBM快10倍）
3. **重计算代替存储**：不存储attention矩阵，需要时重算

**效果**：
- 速度：2-4倍
- 显存：O(N) 而非 O(N²)
- 长序列友好：可以处理更长的上下文

---

### Q9: 数据并行、模型并行、流水线并行有什么区别？

**答案**：

| 并行方式 | 切分对象 | 通信模式 | 适用场景 |
|----------|----------|----------|----------|
| **数据并行** | 数据batch | All-Reduce梯度 | 模型能放入单卡 |
| **模型并行** | 模型层/参数 | 点对点（激活值） | 模型太大单卡放不下 |
| **流水线并行** | 模型层 + micro-batch | 流水线传递 | 超大模型+提高利用率 |

**组合使用**：
```
大规模训练通常结合使用：
- 节点间：数据并行
- 节点内：模型并行 + 流水线并行
```

---

### Q10: ZeRO的三个Stage分别优化什么？

**答案**：

| Stage | 分割内容 | 显存节省 |
|-------|----------|----------|
| **ZeRO-1** | 优化器状态（m, v） | 4倍 |
| **ZeRO-2** | + 梯度 | 8倍 |
| **ZeRO-3** | + 模型参数 | N倍（N=GPU数） |

**ZeRO-3工作流程**：
1. 前向传播：按需收集参数（all-gather）
2. 计算完成：立即释放参数
3. 反向传播：再次收集参数，计算梯度
4. 梯度同步：reduce-scatter

**效果**：8卡训练GPT-3(175B)，每卡只需88GB显存

---

### Q11: 为什么需要学习率预热（Warmup）？

**答案**：

**问题**：
- 训练初期，模型参数随机初始化
- 梯度方向可能很不稳定
- 大学习率会导致参数剧烈震荡

**预热策略**：
```
Step 0:    lr = 1e-7（很小）
Step 500:  lr = 2.5e-5
Step 1000: lr = 5e-5（目标学习率）
之后：线性/余弦衰减
```

**好处**：
- 让模型"热身"，逐步适应
- 避免训练初期梯度爆炸
- 提高训练稳定性

---

### Q12: GPTQ和AWQ有什么区别？如何选择？

**答案**：

| 特性 | GPTQ | AWQ |
|------|------|-----|
| 量化方法 | 基于Hessian的逐层量化 | 基于激活重要性 |
| 量化速度 | 较慢（需计算Hessian） | 较快 |
| 推理速度 | 快 | 更快 |
| 精度 | 高 | 略高 |
| 显存需求 | 中等 | 较低 |

**选择建议**：
- 追求推理速度 → AWQ
- 已有GPTQ生态 → GPTQ
- 一般情况 → 两者都可以，AWQ略优

---

### Q13: 如何选择微调方法（全量/LoRA/QLoRA）？

**答案**：

| 场景 | 推荐方法 | 原因 |
|------|----------|------|
| 充足显存 + 大数据 | 全量微调 | 效果最好 |
| 有限显存 + 中等数据 | LoRA | 平衡效果和成本 |
| 消费级显卡 | QLoRA | 低显存可训练大模型 |
| 多任务部署 | LoRA | 可以切换不同LoRA适配器 |
| 追求极致效果 | 全量微调 | 不受低秩约束 |

**实践建议**：
- 先用QLoRA快速验证可行性
- 效果满意后考虑是否需要全量微调
- 多任务场景用LoRA（热切换）

---

### Q14: vLLM为什么能大幅提升吞吐量？

**答案**：

**三个关键技术**：

1. **PagedAttention**：
   - 将KV Cache分成固定大小的页
   - 动态分配，减少内存碎片
   - 支持更大batch size

2. **Continuous Batching**：
   - 请求完成后立即替换为新请求
   - 不等待整个batch完成
   - 提高GPU利用率

3. **优化的CUDA内核**：
   - 定制化的attention实现
   - 减少kernel launch开销

**效果**：比HuggingFace Transformers快24倍

---

### Q15: 训练大模型时如何避免梯度爆炸/消失？

**答案**：

**梯度爆炸解决方案**：
1. **梯度裁剪**：`clip_grad_norm_(params, max_norm=1.0)`
2. **学习率预热**：避免初期大更新
3. **权重初始化**：合适的初始化（如He初始化）
4. **LayerNorm**：稳定激活值分布

**梯度消失解决方案**：
1. **残差连接**：直接路径传递梯度
2. **适当的激活函数**：GELU/SiLU替代ReLU
3. **Pre-Norm**：LayerNorm放在Attention之前

**监控指标**：
```python
# 监控梯度范数
grad_norm = torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=float('inf'))
if grad_norm > 10:
    print(f"梯度范数过大：{grad_norm}")
```

---

### Q16: 分布式训练中All-Reduce的通信复杂度是多少？

**答案**：

**All-Reduce定义**：
所有节点都有一份数据，需要汇总后每个节点都获得相同的结果。

**算法**：Ring All-Reduce
```
Step 1: Reduce-Scatter（每个节点获得部分和）
Step 2: All-Gather（广播部分和到所有节点）
```

**通信复杂度**：
- 数据量：2 × (N-1)/N × data_size
- 约等于 2 × data_size（N较大时）

**与节点数无关**！这是Ring All-Reduce的优势。

**带宽利用**：
```
理论带宽利用率 = (N-1)/N ≈ 100%（N较大时）
```

---

### Q17: 训练损失下降但线上效果不升反降，通常先查什么？

**答案**：
优先查“离线目标与线上目标是否对齐”。常见问题是训练指标优化了，但线上业务目标（点击、转化、满意度）并未被同等建模。建议先排查：
1. 训练数据分布与线上流量是否漂移。
2. 评测集是否覆盖真实场景。
3. 推理链路是否与训练前处理一致（特征、tokenizer、版本）。

先修目标对齐，再谈继续加训练轮次。

---

### Q18: 微调时怎么判断是“欠拟合”还是“过拟合”？

**答案**：
观察训练/验证曲线和任务样例：
1. **欠拟合**：训练和验证都差，loss 高且下降慢。
2. **过拟合**：训练持续变好，验证变差或震荡。

应对策略：
- 欠拟合：增加训练步数、提高有效 batch、增强数据质量。
- 过拟合：早停、正则化、数据增强、降低学习率。

结论必须基于验证集趋势，不能只看训练集 loss。

---

### Q19: 推理量化后延迟降了，但质量掉得多，怎么做回补？

**答案**：
可以按“分层回补”做：
1. 先做层敏感分析，对关键层回退到高精度（混合精度量化）。
2. 使用更优量化校准集，覆盖真实高频场景。
3. 对关键任务引入 rerank/二阶段模型补偿质量。

量化是系统级权衡，不应只看单一延迟指标。

---

### Q20: 分布式训练频繁 OOM 时，除了减 batch 还有什么工程手段？

**答案**：
常见手段：
1. 开启梯度检查点（activation checkpointing）降低激活显存。
2. 使用 ZeRO/FSDP 分片参数与优化器状态。
3. 混合精度训练（bf16/fp16）减少显存占用。
4. 缩短序列长度或分桶训练，控制峰值内存。

要同时监控吞吐与稳定性，避免“省显存但训练效率崩盘”。

---

**总结**

AI模型训练与优化是LLM工程师的核心技能：
- **Fine-tuning**：LoRA、QLoRA（减少参数量）
- **RLHF**：PPO、DPO（对齐人类偏好）
- **量化**：GPTQ、AWQ（减少模型大小）
- **推理优化**：vLLM、TensorRT、KV Cache、Flash Attention
- **分布式训练**：数据并行、模型并行、ZeRO
