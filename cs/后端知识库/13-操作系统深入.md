# 操作系统深入

---

## 📑 目录

### 进程与内存
1. [进程与线程管理](#1-进程与线程管理)
2. [内存管理](#2-内存管理)

### I/O与文件
3. [IO模型与异步IO](#3-io模型与异步io)
4. [文件系统](#4-文件系统)

### 网络与系统
5. [网络协议栈](#5-网络协议栈)
6. [Cgroup资源隔离](#6-cgroup资源隔离)
7. [系统调用与性能](#7-系统调用与性能)

### 面试题自查
8. [面试题自查](#8-面试题自查)

---

## 1. 进程与线程管理

### 1.1 进程vs线程

| 维度 | 进程 | 线程 |
|------|------|------|
| **定义** | 资源分配的基本单位 | CPU调度的基本单位 |
| **内存** | 独立地址空间 | 共享地址空间 |
| **通信** | IPC（管道、消息队列、共享内存） | 直接访问全局变量 |
| **开销** | 创建/切换开销大（3-5ms） | 创建/切换开销小（<1μs） |
| **稳定性** | 一个进程崩溃不影响其他进程 | 一个线程崩溃整个进程崩溃 |

### 1.2 进程状态转换

```
┌──────┐  fork()  ┌─────────┐  调度  ┌─────────┐
│ 创建 │ ───────→ │ 就绪队列 │ ─────→ │  运行   │
└──────┘          └─────────┘        └─────────┘
                       ↑                   │
                       │                   │ 阻塞（IO/锁）
                       │                   ↓
                       │              ┌─────────┐
                       └──────────────│  阻塞   │
                          唤醒         └─────────┘
```

**查看进程状态**：
```bash
# ps命令
ps aux | head -5
# STAT列：
# R: Running（运行）
# S: Sleeping（睡眠）
# D: Disk Sleep（不可中断的睡眠，等待IO）
# Z: Zombie（僵尸进程）
# T: Stopped（暂停）

# 示例
USER  PID  STAT  COMMAND
root  1    Ss    /sbin/init
root  123  R     ./myapp
root  456  D     cp /data /backup  # IO密集
```

### 1.3 进程调度算法

#### 1.3.1 CFS（Completely Fair Scheduler）
Linux默认调度器，核心思想：**让每个进程获得公平的CPU时间**

```c
// 虚拟运行时间（vruntime）
vruntime += delta_exec * (NICE_0_LOAD / weight)

// weight根据nice值计算：
// nice=-20（最高优先级）：weight=88761
// nice=0（默认）：weight=1024
// nice=19（最低优先级）：weight=15
```

**示例**：
```bash
# 进程A：nice=0, weight=1024
# 进程B：nice=5, weight=335
# 
# 运行10ms后：
# A的vruntime += 10 * (1024/1024) = 10ms
# B的vruntime += 10 * (1024/335) = 30.5ms
# 
# 下次调度选择vruntime最小的（进程A）
```

**调整优先级**：
```bash
# 降低优先级（nice值越大，优先级越低）
nice -n 10 ./myapp

# 提高优先级（需要root）
nice -n -10 ./myapp

# 修改正在运行的进程
renice -n 5 -p 1234
```

#### 1.3.2 实时调度（RT）
```bash
# 查看实时进程
ps -eo pid,class,rtprio,comm | grep RR
# PID  CLS  RTPRIO  COMMAND
# 123  RR   50      realtime-app

# 设置实时优先级（0-99，99最高）
chrt -r -p 50 1234
```

### 1.4 进程间通信（IPC）

#### 1.4.1 管道（Pipe）
```go
// 匿名管道（只能用于父子进程）
cmd := exec.Command("ls", "/var/log")
stdout, _ := cmd.StdoutPipe()
cmd.Start()

scanner := bufio.NewScanner(stdout)
for scanner.Scan() {
    fmt.Println(scanner.Text())
}
```

**命名管道（FIFO）**：
```bash
# 创建命名管道
mkfifo /tmp/mypipe

# 进程A：写入
echo "hello" > /tmp/mypipe

# 进程B：读取
cat /tmp/mypipe  # 输出: hello
```

#### 1.4.2 共享内存（最快的IPC）
```c
#include <sys/shm.h>

// 进程A：创建共享内存
int shmid = shmget(IPC_PRIVATE, 1024, IPC_CREAT | 0666);
char *shmaddr = (char *)shmat(shmid, NULL, 0);
strcpy(shmaddr, "hello from process A");

// 进程B：读取共享内存
char *shmaddr = (char *)shmat(shmid, NULL, 0);
printf("%s\n", shmaddr);  // 输出: hello from process A

// 清理
shmdt(shmaddr);
shmctl(shmid, IPC_RMID, NULL);
```

**Go语言使用mmap**：
```go
import "golang.org/x/exp/mmap"

// 进程A：写入
f, _ := os.Create("/tmp/shared.dat")
f.Write([]byte("hello"))
f.Close()

// 进程B：mmap读取
at, _ := mmap.Open("/tmp/shared.dat")
data := make([]byte, 5)
at.ReadAt(data, 0)
fmt.Println(string(data))  // 输出: hello
```

#### 1.4.3 信号（Signal）
```bash
# 常用信号
SIGTERM (15)  # 优雅退出
SIGKILL (9)   # 强制杀死（无法捕获）
SIGHUP (1)    # 终端断开
SIGUSR1 (10)  # 用户自定义

# 发送信号
kill -15 1234  # 发送SIGTERM
kill -9 1234   # 发送SIGKILL
```

**Go语言捕获信号**：
```go
import (
    "os"
    "os/signal"
    "syscall"
)

func main() {
    sigChan := make(chan os.Signal, 1)
    signal.Notify(sigChan, syscall.SIGTERM, syscall.SIGINT)
    
    go func() {
        <-sigChan
        log.Println("收到退出信号，开始清理资源...")
        // 关闭数据库连接、保存状态等
        cleanup()
        os.Exit(0)
    }()
    
    // 主逻辑
    run()
}
```

### 1.5 线程模型

#### 1.5.1 用户级线程 vs 内核级线程

| 类型 | 实现 | 优点 | 缺点 |
|------|------|------|------|
| **用户级线程** | 用户空间调度（Go runtime） | 轻量、切换快 | 无法利用多核 |
| **内核级线程** | 内核调度（pthread） | 利用多核 | 创建/切换开销大 |
| **混合模型** | M:N映射（Go的M:P:G） | 兼顾两者 | 实现复杂 |

#### 1.5.2 Go的GMP模型

```
┌─────────────────────────────────────┐
│         G (Goroutine)               │
│  轻量级线程，2KB初始栈               │
└──────────┬──────────────────────────┘
           │ 调度
┌──────────↓──────────────────────────┐
│         P (Processor)               │
│  逻辑处理器，GOMAXPROCS数量          │
└──────────┬──────────────────────────┘
           │ 绑定
┌──────────↓──────────────────────────┐
│         M (Machine)                 │
│  内核线程，真正执行代码               │
└─────────────────────────────────────┘
```

**工作窃取（Work Stealing）**：
```go
// P1的G队列：[G1, G2, G3]
// P2的G队列：[]（空闲）

// P2会从P1偷一半任务
// P1的G队列：[G1]
// P2的G队列：[G2, G3]
```

**查看Goroutine数量**：
```go
import "runtime"

func main() {
    fmt.Println("Goroutines:", runtime.NumGoroutine())
    
    // 创建10000个goroutine
    for i := 0; i < 10000; i++ {
        go func() {
            time.Sleep(1 * time.Hour)
        }()
    }
    
    fmt.Println("Goroutines:", runtime.NumGoroutine())  // 输出: 10001
}
```

### 1.6 僵尸进程与孤儿进程

**僵尸进程（Zombie）**：

```
产生原因：
- 子进程退出后，父进程未调用wait()回收

状态：
- 进程已结束，但进程表项未删除
- 占用PID资源（PID有限）

查看僵尸进程：
ps aux | grep Z
# USER  PID  STAT  COMMAND
# root  123  Z     [myapp] <defunct>

解决方法：
1. 父进程调用wait()/waitpid()
2. 杀死父进程（僵尸进程变成孤儿进程，由init回收）
```

**孤儿进程**：

```
产生原因：
- 父进程退出，子进程仍在运行

处理方式：
- 由init进程（PID=1）收养
- init进程会调用wait()回收

孤儿进程是正常的，不会造成资源泄漏
```

**Go代码示例**：

```go
// ❌ 可能产生僵尸进程
func badExample() {
    cmd := exec.Command("sleep", "10")
    cmd.Start()
    // 忘记调用 cmd.Wait()
}

// ✅ 正确做法
func goodExample() {
    cmd := exec.Command("sleep", "10")
    cmd.Start()
    cmd.Wait()  // 回收子进程
}
```

---

### 1.7 上下文切换

**什么是上下文切换？**

```
进程上下文切换：
1. 保存当前进程的CPU寄存器、程序计数器、栈指针
2. 保存页表基址（TLB失效！）
3. 切换到新进程的地址空间
4. 恢复新进程的CPU状态

开销：约3-5微秒（主要是TLB失效）

线程上下文切换：
- 同一进程内切换，无需切换地址空间
- TLB不失效
- 开销约1微秒
```

**查看上下文切换次数**：

```bash
# 系统级别
vmstat 1
# procs -----------memory---------- ---swap-- -----io---- -system-- ------cpu-----
#  r  b   swpd   free   buff  cache   si   so    bi    bo   in   cs us sy id wa st
#  1  0      0 1234567  1234  12345    0    0     0     0  100  500  5  2 93  0  0
#                                                              ↑
#                                                      context switches/s

# 进程级别
pidstat -w -p 1234 1
# PID   cswch/s nvcswch/s  Command
# 1234  100     50         myapp
#       ↑       ↑
#       自愿切换（等待IO/锁）
#               非自愿切换（时间片用完）
```

**减少上下文切换**：

```go
// ❌ 频繁切换（阻塞式）
for i := 0; i < 10000; i++ {
    go func() {
        time.Sleep(1 * time.Second)  // 每次都切换
    }()
}

// ✅ 批量处理（减少切换）
go func() {
    for i := 0; i < 10000; i++ {
        // 批量处理，减少goroutine数量
    }
}()
```

---

### 1.8 CPU缓存与内存屏障⭐⭐

**CPU缓存层级**：

```
CPU核心
  ↓
L1 Cache（32-64KB，1-2周期）← 每核独占
  ↓
L2 Cache（256KB-1MB，10周期）← 每核独占
  ↓
L3 Cache（8-64MB，30周期）← 多核共享
  ↓
主内存（100周期）

速度：L1 > L2 > L3 > 内存
延迟：1ns < 4ns < 15ns < 100ns
```

**缓存行（Cache Line）**：

```
- 缓存的最小单位：64字节
- 读取一个字节，会加载整个缓存行

伪共享（False Sharing）问题：
// ❌ 两个变量在同一缓存行，频繁失效
type Counter struct {
    a int64  // 8字节
    b int64  // 8字节
}
// a和b可能在同一缓存行（64字节）
// 线程1修改a，导致线程2的b缓存失效

// ✅ 缓存行填充（Padding）
type Counter struct {
    a int64
    _ [56]byte  // 填充到64字节
    b int64
    _ [56]byte
}
```

**MESI缓存一致性协议**：

```
四种状态：
M (Modified)：已修改，独占，需写回
E (Exclusive)：独占，未修改
S (Shared)：共享，只读
I (Invalid)：无效

状态转换示例：
1. CPU0读取变量X → X在CPU0为E状态
2. CPU1读取变量X → X在CPU0、CPU1都为S状态
3. CPU0修改变量X → X在CPU0为M状态，CPU1为I状态
4. CPU1读取变量X → CPU0写回内存，X在两者都为S状态
```

**内存屏障（Memory Barrier）**：

```go
// CPU和编译器可能重排指令

// 示例：双重检查锁定（DCL）
var instance *Singleton
var once sync.Once

func GetInstance() *Singleton {
    once.Do(func() {
        instance = &Singleton{}  // 可能重排为：分配内存→赋值→初始化
    })
    return instance
}

// Go的sync.Once内部使用atomic操作保证可见性
// atomic.StorePointer 隐含内存屏障
```

**查看缓存命中率**：

```bash
perf stat -e cache-references,cache-misses ./myapp
# 1,000,000 cache-references
# 50,000    cache-misses   # 5% miss rate
```

---

## 2. 内存管理

### 2.1 虚拟内存

**为什么需要虚拟内存？**
1. **隔离**：每个进程有独立地址空间，互不干扰
2. **扩展**：虚拟内存可以大于物理内存（通过swap）
3. **共享**：多个进程共享同一物理页（如libc.so）

**地址转换**：
```
虚拟地址 (48位) = 页号 (36位) + 页内偏移 (12位)
                  ↓ 查页表
物理地址 (48位) = 页框号 (36位) + 页内偏移 (12位)
```

**查看进程内存布局**：
```bash
cat /proc/1234/maps
# 输出：
# 地址范围              权限  偏移  设备  inode  路径
# 00400000-00452000  r-xp  0     08:01  123    /usr/bin/myapp  # 代码段
# 00651000-00652000  r--p  51000 08:01  123    /usr/bin/myapp  # 只读数据
# 00652000-00653000  rw-p  52000 08:01  123    /usr/bin/myapp  # 读写数据
# 7ffff7a0d000-...   r-xp  0     08:01  456    /lib/libc.so.6  # 共享库
# 7ffffffde000-...   rw-p  0     00:00  0      [stack]         # 栈
# 7ffff7ff9000-...   rw-p  0     00:00  0      [heap]          # 堆
```

### 2.2 页表与TLB

**多级页表（x86_64）**：
```
虚拟地址 (48位)
├─ PGD索引 (9位) ────→ 页全局目录
├─ PUD索引 (9位) ────→ 页上层目录
├─ PMD索引 (9位) ────→ 页中间目录
├─ PTE索引 (9位) ────→ 页表项
└─ 页内偏移 (12位) ──→ 物理页内偏移
```

**TLB（Translation Lookaside Buffer）**：
- 缓存虚拟地址→物理地址的映射
- 命中时无需查页表（快100倍）
- TLB Miss时触发Page Walk（查多级页表）

**查看TLB Miss率**：
```bash
perf stat -e dTLB-load-misses,dTLB-loads ./myapp
# 输出：
# 1,234,567  dTLB-loads
# 12,345     dTLB-load-misses  # Miss率=1%
```

**优化TLB Miss**：
```c
// ❌ 跨步访问（TLB Miss多）
for (int i = 0; i < N; i += 1024) {
    sum += arr[i];
}

// ✅ 顺序访问（TLB命中高）
for (int i = 0; i < N; i++) {
    sum += arr[i];
}
```

### 2.3 缺页中断（Page Fault）

**分类**：
1. **Minor Page Fault**：页在内存中，但未映射到页表
2. **Major Page Fault**：页不在内存，需要从磁盘读取

**查看Page Fault**：
```bash
ps -o pid,min_flt,maj_flt,comm
# PID  MINFLT  MAJFLT  COMMAND
# 1234  12345   10      myapp

# strace跟踪
strace -c ./myapp
# % time  calls  errors  syscall
# 45.00   10     0       mmap      # 内存映射
# 30.00   10     0       page_fault # 缺页中断
```

**减少Page Fault**：
```go
// 预分配内存，减少缺页中断
slice := make([]int, 0, 1000000)  // 预分配100万个元素的容量
for i := 0; i < 1000000; i++ {
    slice = append(slice, i)
}
```

### 2.4 内存分配器

#### 2.4.1 Buddy System（Linux）
```
伙伴系统：内存块大小为2^n（4KB、8KB、16KB...）

示例：申请6KB
1. 找到最小的满足条件的块（8KB）
2. 如果没有8KB，则分裂16KB块 → 两个8KB块
3. 分配一个8KB块，另一个8KB块放回空闲链表
```

**查看内存碎片**：
```bash
cat /proc/buddyinfo
# Node 0, zone DMA      1   1   1   0   2   1   1   0   1   1   3
# Node 0, zone Normal   100 50  20  10  5   2   1   0   0   0   0
#                       4KB 8KB 16K 32K 64K 128K...
```

#### 2.4.2 TCMalloc（Thread-Caching Malloc）
Go、Chrome使用

```
┌────────────────────────┐
│   Thread Cache         │  线程私有，无锁
│  小对象(<32KB)快速分配  │
└───────┬────────────────┘
        │ 不够时申请
┌───────↓────────────────┐
│   Central Cache        │  全局，有锁
│  中等对象(32KB-256KB)   │
└───────┬────────────────┘
        │ 不够时申请
┌───────↓────────────────┐
│   Page Heap            │  全局，管理大块内存
│  大对象(>256KB)         │
└────────────────────────┘
```

**优势**：
- 小对象分配无锁（线程缓存）
- 减少内存碎片（size class）

### 2.5 内存泄漏排查

#### 2.5.1 Go内存泄漏
```go
// ❌ Goroutine泄漏
func leak() {
    ch := make(chan int)
    go func() {
        <-ch  // 永远阻塞，goroutine泄漏
    }()
    // 忘记关闭channel
}

// ✅ 正确做法
func noLeak() {
    ch := make(chan int)
    go func() {
        select {
        case <-ch:
        case <-time.After(5 * time.Second):
            return  // 超时退出
        }
    }()
    close(ch)
}
```

**pprof排查**：
```bash
# 1. 启用pprof
import _ "net/http/pprof"
go http.ListenAndServe(":6060", nil)

# 2. 查看heap
go tool pprof http://localhost:6060/debug/pprof/heap

# 3. 对比两次快照
curl http://localhost:6060/debug/pprof/heap > heap1.prof
# 等待一段时间
curl http://localhost:6060/debug/pprof/heap > heap2.prof

go tool pprof -base heap1.prof heap2.prof
(pprof) top
# 显示增长最多的内存分配点
```

#### 2.5.2 C/C++内存泄漏
```bash
# Valgrind检测
valgrind --leak-check=full ./myapp

# 输出：
# ==1234== HEAP SUMMARY:
# ==1234==     in use at exit: 1,000 bytes in 1 blocks
# ==1234==   total heap usage: 1 allocs, 0 frees, 1,000 bytes allocated
# ==1234== 
# ==1234== 1,000 bytes in 1 blocks are definitely lost
# ==1234==    at malloc (vg_replace_malloc.c:299)
# ==1234==    by main (main.c:10)
```

---

## 3. IO模型与异步IO

### 3.1 五种IO模型

| 模型 | 阻塞 | 说明 |
|------|------|------|
| **阻塞IO** | ✅ | read()阻塞直到数据到达 |
| **非阻塞IO** | ❌ | read()立即返回，需轮询 |
| **IO多路复用** | ✅ | select/epoll阻塞，监听多个fd |
| **信号驱动IO** | ❌ | 数据到达时发送信号 |
| **异步IO** | ❌ | 内核完成IO后通知应用 |

#### 3.1.1 阻塞IO
```c
// 阻塞直到收到数据
char buf[1024];
int n = read(sockfd, buf, sizeof(buf));  // 阻塞
printf("received: %s\n", buf);
```

#### 3.1.2 非阻塞IO
```c
// 设置为非阻塞
fcntl(sockfd, F_SETFL, O_NONBLOCK);

// 轮询
while (1) {
    int n = read(sockfd, buf, sizeof(buf));
    if (n > 0) {
        printf("received: %s\n", buf);
        break;
    } else if (errno == EAGAIN) {
        // 数据还没到，继续轮询
        usleep(1000);
    }
}
```

#### 3.1.3 IO多路复用（epoll）
```c
int epfd = epoll_create1(0);

// 添加监听的fd
struct epoll_event ev;
ev.events = EPOLLIN;
ev.data.fd = sockfd;
epoll_ctl(epfd, EPOLL_CTL_ADD, sockfd, &ev);

// 等待事件
struct epoll_event events[10];
while (1) {
    int nfds = epoll_wait(epfd, events, 10, -1);  // 阻塞
    for (int i = 0; i < nfds; i++) {
        if (events[i].events & EPOLLIN) {
            // 可读
            int fd = events[i].data.fd;
            read(fd, buf, sizeof(buf));
        }
    }
}
```

**Go的netpoller**：
```go
// Go运行时自动使用epoll（Linux）/kqueue（macOS）
listener, _ := net.Listen("tcp", ":8080")
for {
    conn, _ := listener.Accept()  // 底层使用epoll
    go handleConn(conn)
}
```

### 3.2 epoll vs select

| 特性 | select | epoll |
|------|--------|-------|
| **fd数量限制** | 1024 | 无限制 |
| **性能** | O(n) | O(1) |
| **内存拷贝** | 每次拷贝fd_set | 共享内存 |
| **适用场景** | 少量连接 | 高并发（C10K） |

**select缺陷**：
```c
fd_set readfds;
FD_ZERO(&readfds);
FD_SET(fd1, &readfds);
FD_SET(fd2, &readfds);
// ... 添加1000个fd

select(maxfd+1, &readfds, NULL, NULL, NULL);

// 需要遍历所有fd，找到就绪的（O(n)）
for (int fd = 0; fd <= maxfd; fd++) {
    if (FD_ISSET(fd, &readfds)) {
        read(fd, buf, sizeof(buf));
    }
}
```

**epoll优化**：
```c
epoll_wait(epfd, events, 10, -1);  // 返回就绪的events

// 只遍历就绪的fd（O(1)）
for (int i = 0; i < nfds; i++) {
    int fd = events[i].data.fd;
    read(fd, buf, sizeof(buf));
}
```

### 3.3 零拷贝（Zero Copy）

#### 3.3.1 传统拷贝（4次拷贝）
```
磁盘 → 内核缓冲区（DMA拷贝）
     → 用户空间（CPU拷贝）
     → Socket缓冲区（CPU拷贝）
     → 网卡（DMA拷贝）
```

#### 3.3.2 sendfile（2次拷贝）
```c
// Linux sendfile
int filefd = open("file.txt", O_RDONLY);
int sockfd = socket(AF_INET, SOCK_STREAM, 0);

// 直接从文件fd发送到socket fd（零拷贝）
sendfile(sockfd, filefd, NULL, file_size);

// 内核直接传输，无需拷贝到用户空间
// 磁盘 → 内核缓冲区 → 网卡
```

**Go语言**：
```go
// Go自动使用sendfile优化（Linux）
func ServeFile(w http.ResponseWriter, r *http.Request) {
    f, _ := os.Open("large_file.dat")
    defer f.Close()
    io.Copy(w, f)  // 底层使用sendfile
}
```

#### 3.3.3 mmap（内存映射）
```c
// 将文件映射到内存
int fd = open("file.txt", O_RDONLY);
char *data = mmap(NULL, file_size, PROT_READ, MAP_PRIVATE, fd, 0);

// 直接访问，像访问内存一样
printf("%s\n", data);  // 无需read()

munmap(data, file_size);
```

**优点**：
- 避免read()系统调用
- 多个进程共享同一物理页
- 内核自动管理缓存

**Nginx使用mmap**：
```nginx
location /static/ {
    # 开启sendfile和mmap
    sendfile on;
    tcp_nopush on;
}
```

### 3.4 直接IO（Direct IO）

**绕过页缓存，直接读写磁盘**：
```c
int fd = open("file.txt", O_DIRECT | O_RDONLY);
// 要求：buf地址对齐、大小为512字节的倍数
char *buf = aligned_alloc(512, 4096);
read(fd, buf, 4096);
```

**使用场景**：
- 数据库（自己管理缓存，不需要页缓存）
- 流媒体（大文件，不需要缓存）

**对比**：
```bash
# 普通IO（使用页缓存）
dd if=/dev/zero of=test bs=1M count=1000
# 1000+0 records in
# 1000+0 records out
# 1048576000 bytes (1.0 GB) copied, 0.5 s, 2.0 GB/s

# Direct IO（绕过缓存）
dd if=/dev/zero of=test oflag=direct bs=1M count=1000
# 1000+0 records in
# 1000+0 records out
# 1048576000 bytes (1.0 GB) copied, 2.0 s, 500 MB/s
```

---

## 4. 文件系统

### 4.1 inode与目录结构

**inode（索引节点）**：
```bash
ls -li /etc/hosts
# 123456 -rw-r--r-- 1 root root 123 Jan 1 00:00 /etc/hosts
#   ↑ inode号

stat /etc/hosts
# File: /etc/hosts
# Inode: 123456
# Links: 1
# Access: (0644/-rw-r--r--)
# Size: 123
# Blocks: 8
```

**inode包含的信息**：
- 文件类型（普通文件、目录、符号链接）
- 权限
- 所有者
- 大小
- 时间戳（atime、mtime、ctime）
- **数据块指针**（指向实际数据的位置）

**硬链接vs软链接**：
```bash
# 硬链接：多个文件名指向同一个inode
ln /etc/hosts /tmp/hosts_link
ls -li /etc/hosts /tmp/hosts_link
# 123456 -rw-r--r-- 2 root root 123 Jan 1 00:00 /etc/hosts
# 123456 -rw-r--r-- 2 root root 123 Jan 1 00:00 /tmp/hosts_link
# 同一个inode，Links=2

# 软链接：新文件，inode不同，内容是目标路径
ln -s /etc/hosts /tmp/hosts_symlink
ls -li /tmp/hosts_symlink
# 789012 lrwxrwxrwx 1 root root 10 Jan 1 00:00 /tmp/hosts_symlink -> /etc/hosts
```

### 4.2 文件系统布局（ext4）

```
┌──────────────────────────────────────────┐
│  Super Block（超级块）                    │
│  - 文件系统大小、块大小、inode数量等      │
└──────────────────────────────────────────┘
┌──────────────────────────────────────────┐
│  Block Group Descriptor（块组描述符）     │
│  - 每个块组的inode表、数据块位置          │
└──────────────────────────────────────────┘
┌──────────────────────────────────────────┐
│  Inode Bitmap（inode位图）                │
│  - 标记哪些inode空闲                      │
└──────────────────────────────────────────┘
┌──────────────────────────────────────────┐
│  Block Bitmap（块位图）                   │
│  - 标记哪些数据块空闲                     │
└──────────────────────────────────────────┘
┌──────────────────────────────────────────┐
│  Inode Table（inode表）                   │
│  - 存储所有inode                          │
└──────────────────────────────────────────┘
┌──────────────────────────────────────────┐
│  Data Blocks（数据块）                    │
│  - 实际文件内容                           │
└──────────────────────────────────────────┘
```

**查看文件系统信息**：
```bash
dumpe2fs /dev/sda1 | head -20
# Block count: 26214400
# Block size: 4096
# Inode count: 6553600
# Free blocks: 20000000
# Free inodes: 6000000
```

### 4.3 文件IO缓冲

#### 4.3.1 用户空间缓冲（stdio）
```c
// fwrite()：先写到用户空间缓冲区
FILE *fp = fopen("file.txt", "w");
fwrite("hello", 5, 1, fp);  // 写到用户缓冲区
// 此时还没写到内核

fflush(fp);  // 刷新到内核
fclose(fp);  // 关闭时自动fflush
```

#### 4.3.2 内核空间缓冲（page cache）
```c
// write()：写到内核页缓存
int fd = open("file.txt", O_WRONLY);
write(fd, "hello", 5);  // 写到page cache
// 此时还没写到磁盘

fsync(fd);  // 强制刷新到磁盘
close(fd);
```

**查看page cache**：
```bash
free -h
#               total  used   free   shared  buff/cache  available
# Mem:          16Gi   2Gi    8Gi    100Mi   6Gi         13Gi
#                                             ↑ page cache

# 清空page cache（测试用，生产慎用）
sync
echo 3 > /proc/sys/vm/drop_caches
```

### 4.4 文件系统性能优化

#### 4.4.1 批量操作
```go
// ❌ 逐条写入（10000次系统调用）
for _, line := range lines {
    file.WriteString(line + "\n")
}

// ✅ 批量写入（1次系统调用）
var buf bytes.Buffer
for _, line := range lines {
    buf.WriteString(line + "\n")
}
file.Write(buf.Bytes())
```

#### 4.4.2 异步刷盘
```go
// ❌ 每次都fsync（慢）
file.Write(data)
file.Sync()  // 每次都刷盘

// ✅ 定时刷盘
ticker := time.NewTicker(1 * time.Second)
go func() {
    for range ticker.C {
        file.Sync()  // 每秒刷盘一次
    }
}()
```

---

## 5. 网络协议栈

### 5.1 数据包在内核中的流转

```
应用层：send() → socket缓冲区
         ↓
传输层：TCP分段、添加TCP头
         ↓
网络层：添加IP头、路由查找
         ↓
链路层：添加MAC头、ARP解析
         ↓
物理层：网卡驱动、DMA传输
```

**查看网络栈统计**：
```bash
netstat -s
# Tcp:
#     12345 active connections openings
#     67890 passive connection openings
#     100 failed connection attempts
#     200 connection resets received
#     300 segments retransmited

# 重传率 = 300 / (12345+67890) = 0.4%
```

### 5.2 TCP连接建立（三次握手）

```
客户端                          服务端
  │                               │
  │ ─────── SYN (seq=x) ────────→ │  SYN_SENT
  │                               │
  │ ←─── SYN+ACK (seq=y, ack=x+1) │  SYN_RCVD
  │                               │
  │ ─────── ACK (ack=y+1) ──────→ │  ESTABLISHED
  │                               │
```

**抓包验证**：
```bash
tcpdump -i eth0 port 80 -nn
# 14:01:00.123456 IP 10.0.1.2.12345 > 1.70.84.48.80: Flags [S], seq 1000
# 14:01:00.123789 IP 1.70.84.48.80 > 10.0.1.2.12345: Flags [S.], seq 2000, ack 1001
# 14:01:00.124000 IP 10.0.1.2.12345 > 1.70.84.48.80: Flags [.], ack 2001
```

**半连接队列与全连接队列**：
```bash
# 查看队列大小
ss -lnt
# State   Recv-Q  Send-Q  Local Address:Port
# LISTEN  0       128     0.0.0.0:80
#         ↑       ↑
#         当前全连接队列长度
#                 全连接队列最大长度

# 调整队列大小
sysctl -w net.core.somaxconn=4096        # 全连接队列
sysctl -w net.ipv4.tcp_max_syn_backlog=8192  # 半连接队列
```

### 5.3 TCP拥塞控制

#### 5.3.1 拥塞窗口（cwnd）
```
慢启动：cwnd = 1, 2, 4, 8, 16...（指数增长）
拥塞避免：cwnd = cwnd + 1（线性增长）
快速重传：收到3个重复ACK，立即重传
快速恢复：cwnd减半，继续传输
```

**查看cwnd**：
```bash
ss -i
# tcp   ESTAB  0  0  10.0.1.2:12345  1.70.84.48:80
#      cubic wscale:7,7 rto:200 rtt:12.5/7.5 cwnd:10 ssthresh:20
#      ↑ 拥塞控制算法
#                                              ↑ 拥塞窗口
```

#### 5.3.2 BBR算法（Google）
```bash
# 启用BBR
sysctl -w net.ipv4.tcp_congestion_control=bbr

# 对比Cubic vs BBR
# Cubic：基于丢包判断拥塞
# BBR：基于带宽和RTT，更激进
```

### 5.4 Socket参数优化

```go
// 1. SO_REUSEADDR：快速重启服务
listener, _ := net.Listen("tcp", ":8080")
file, _ := listener.(*net.TCPListener).File()
syscall.SetsockoptInt(int(file.Fd()), syscall.SOL_SOCKET, syscall.SO_REUSEADDR, 1)

// 2. TCP_NODELAY：禁用Nagle算法（减少延迟）
conn.(*net.TCPConn).SetNoDelay(true)

// 3. SO_KEEPALIVE：保活检测
conn.(*net.TCPConn).SetKeepAlive(true)
conn.(*net.TCPConn).SetKeepAlivePeriod(30 * time.Second)

// 4. SO_RCVBUF/SO_SNDBUF：调整缓冲区
file, _ := conn.(*net.TCPConn).File()
syscall.SetsockoptInt(int(file.Fd()), syscall.SOL_SOCKET, syscall.SO_RCVBUF, 1024*1024)  // 1MB
syscall.SetsockoptInt(int(file.Fd()), syscall.SOL_SOCKET, syscall.SO_SNDBUF, 1024*1024)
```

---

## 6. Cgroup资源隔离

> **Cgroup（Control Groups）** 是Linux内核提供的资源限制、控制与隔离机制，是容器技术（Docker/K8s）的核心基石。

### 6.1 Cgroup概述

**四大资源子系统**：
| 子系统 | 控制对象 | 关键文件/工具 |
|--------|----------|---------------|
| **CPU** | 计算时间、核心绑定 | cpuset, cpu.cfs_quota_us, cpu.shares |
| **Memory** | 内存用量、Swap | memory.limit_in_bytes, memory.oom_control |
| **BlkIO** | 磁盘带宽、IOPS | blkio.throttle.*, blkio.weight |
| **Net** | 网络带宽 | net_cls + tc（流量控制） |

**查看系统Cgroup版本**：
```bash
# Cgroup V1（传统，各子系统独立挂载）
mount | grep cgroup
# cgroup on /sys/fs/cgroup/cpu type cgroup (rw,...,cpu,cpuacct)
# cgroup on /sys/fs/cgroup/memory type cgroup (rw,...,memory)

# Cgroup V2（unified hierarchy，单一挂载点）
mount | grep cgroup2
# cgroup2 on /sys/fs/cgroup type cgroup2 (rw,...,nsdelegate)
```

### 6.2 CPU资源隔离

Linux通过CFS（完全公平调度器）实现CPU时间的分配，Cgroup提供三种隔离方式：

#### 6.2.1 cpuset — 核心绑定

将进程绑定到指定CPU核心运行，实现**物理隔离**：

```bash
# 查看/设置cgroup绑定的CPU核心
cat /sys/fs/cgroup/cpuset/mygroup/cpuset.cpus
echo "0-3" > /sys/fs/cgroup/cpuset/mygroup/cpuset.cpus

# 必须同时设置内存节点（NUMA）
echo "0" > /sys/fs/cgroup/cpuset/mygroup/cpuset.mems
```

**超线程陷阱**：在开启超线程的服务器上，需注意核心编号的物理对应关系：
```bash
# 查看物理核心与逻辑核心的映射
cat /proc/cpuinfo | grep -e "core id" -e "physical id"
# physical id: 0, core id: 0  → 逻辑核心 0 和 12 是同一物理核心
# 绑定 0-5 和 12-17 才是"半数物理核心"
# 绑定 0-11 看似一半，实际覆盖了所有物理核心（超线程部分无法倍增算力）
```

**实验数据对比**（24逻辑核心服务器，筛质数测试）：
| 配置 | 核心数 | 执行时间 |
|------|--------|----------|
| 0-23（全部） | 24 | 8.9s |
| 0-11（全物理核心） | 12 | 9.5s ≈ 无变化！ |
| 0-5,12-17（半物理核心） | 12 | 17.8s ≈ 翻倍 |

#### 6.2.2 cpuquota — 配额限制（硬限制）

按时间比例限制CPU使用上限，**即使系统空闲也不能超额**：

```bash
# 时间周期（默认100ms = 100000μs）
cat /sys/fs/cgroup/cpu/mygroup/cpu.cfs_period_us   # 100000

# 配额（-1表示不限制）
# 限制单核50%：echo 50000
# 限制4核满载：echo 400000
# 限制24核50%：echo 1200000
echo "50000" > /sys/fs/cgroup/cpu/mygroup/cpu.cfs_quota_us
```

**计算公式**：`quota = 目标百分比 × period × CPU核心数`

#### 6.2.3 cpushares — 权重比例（弹性限制）

按权重比例分配CPU时间，**系统空闲时可借用其他cgroup的资源**：

```bash
# 权重范围：2-262144，默认1024
echo "1024" > /sys/fs/cgroup/cpu/groupA/cpu.shares  # A
echo "2048" > /sys/fs/cgroup/cpu/groupB/cpu.shares  # B

# 效果：
# 竞争时：A占1/3，B占2/3
# B空闲时：A可占满所有CPU
```

**三种方式对比**：
| 特性 | cpuset | cpuquota | cpushares |
|------|--------|----------|-----------|
| **隔离类型** | 物理核心隔离 | 时间配额硬限制 | 时间权重弹性分配 |
| **空闲借用** | ❌ 不能 | ❌ 不能 | ✅ 可以 |
| **最小粒度** | 1个核心 | 任意百分比 | 任意比例 |
| **隔离强度** | 最强（核心独占） | 强（硬上限） | 弱（按比例） |
| **适用场景** | 延迟敏感型、NUMA | 多租户硬隔离 | 弹性共享、超卖 |

**内核资源冲突的影响**：当多个cgroup的进程访问相同的内核资源（如同一文件的自旋锁竞争），cpuset由于物理隔离表现最优，cpuquota在此场景下性能损失最大。

### 6.3 内存资源隔离

#### 6.3.1 Linux内存管理基础回顾

```
进程申请内存 → malloc/mmap（仅分配虚拟地址）
     ↓ 实际访问时
缺页异常（Page Fault）
     ├─ Minor Fault：页在内存但未映射（无磁盘IO）
     └─ Major Fault：页在Swap/磁盘（需磁盘IO）
     ↓
alloc_pages → 伙伴系统分配物理页
     ↓ 内存不足时进入 slowpath
1. 唤醒 kswapd 交换守护进程
2. 尝试回收 Page Cache（进程置D状态）
3. 触发 OOM Killer 杀进程
4. 循环重试
```

#### 6.3.2 内存限制配置

```bash
# 硬限制：内存使用上限（超出触发OOM或挂起）
echo "512M" > /sys/fs/cgroup/memory/mygroup/memory.limit_in_bytes

# 内存+Swap总量限制（设为与limit相同 = 禁用Swap）
echo "512M" > /sys/fs/cgroup/memory/mygroup/memory.memsw.limit_in_bytes

# 软限制：用于内存超卖场景，全局内存紧张时按此值回收
echo "256M" > /sys/fs/cgroup/memory/mygroup/memory.soft_limit_in_bytes

# 查看当前使用量
cat /sys/fs/cgroup/memory/mygroup/memory.usage_in_bytes
```

#### 6.3.3 OOM控制

```bash
cat /sys/fs/cgroup/memory/mygroup/memory.oom_control
# oom_kill_disable 0   → 0=启用OOM Killer（默认），1=禁用（进程挂起为D状态）
# under_oom 0          → 0=正常，1=当前已OOM

# 禁用OOM Killer（进程挂起而非被杀）
echo 1 > /sys/fs/cgroup/memory/mygroup/memory.oom_control
# 恢复方式：增加内存限制、杀进程、迁移进程到其他cgroup
```

#### 6.3.4 内存压力通知（eventfd）

```c
// 通过 eventfd + memory.pressure_level 实现内存压力监控
int efd = eventfd(0, 0);
int cfd = open("/sys/fs/cgroup/memory/mygroup/memory.pressure_level", O_RDONLY);
// 写入 cgroup.event_control: "<efd> <cfd> <level>"
// level: "low" | "medium" | "critical"

// 压力级别：
// low     → 开始内存回收
// medium  → 开始Swap换出、活跃Cache写回
// critical → 触发OOM Killer
```

### 6.4 IO资源隔离

#### 6.4.1 Linux IO体系7层架构

```
应用程序 read()/write()
    ↓
① VFS层         — 虚拟文件系统，统一接口
    ↓
② 文件系统层    — ext4/xfs/btrfs 具体实现
    ↓
③ 页缓存层      — Page Cache（Buffered IO在此缓冲）
    ↓
④ 通用块层      — Block IO请求标准化 ← 【带宽/IOPS限速在此实现】
    ↓
⑤ IO调度层      — cfq/deadline/noop ← 【权重限速在此实现】
    ↓
⑥ 块设备驱动层  — 设备操作接口
    ↓
⑦ 块设备层      — 物理设备
```

#### 6.4.2 IO调度算法

| 算法 | 特点 | 适用场景 |
|------|------|----------|
| **cfq** | 完全公平，按进程分时间片 | 通用服务器，支持cgroup权重 |
| **deadline** | 保证请求最终期限，防止饿死 | 数据库等IO密集型 |
| **noop** | 无调度，仅FIFO合并 | SSD固态硬盘（最佳） |

```bash
# 查看/切换调度算法
cat /sys/block/sda/queue/scheduler    # noop deadline [cfq]
echo deadline > /sys/block/sda/queue/scheduler
```

#### 6.4.3 blkio限速配置

```bash
# 带宽限制（基于通用块层，需 Direct IO 才对应用可感知）
echo "8:0 1048576" > /sys/fs/cgroup/blkio/mygroup/blkio.throttle.write_bps_device  # 写1MB/s
echo "8:0 1048576" > /sys/fs/cgroup/blkio/mygroup/blkio.throttle.read_bps_device   # 读1MB/s

# IOPS限制
echo "8:0 100" > /sys/fs/cgroup/blkio/mygroup/blkio.throttle.write_iops_device

# 权重限制（需cfq调度器，同样需 Direct IO）
echo 500 > /sys/fs/cgroup/blkio/mygroup/blkio.weight  # 范围10-1000
```

**重要限制**：Cgroup V1的blkio对 **Buffered IO无法限速**！因为Buffered IO的write-back由内核异步完成，无法追踪所属cgroup。此问题在Cgroup V2中已解决。

### 6.5 网络资源隔离

Cgroup网络隔离复用Linux的 **tc（Traffic Control）** 机制，通过 `net_cls` 子系统给数据包打标签，配合tc队列规则实现限速。

#### 6.5.1 tc队列规则（qdisc）概览

| 类型 | qdisc | 核心机制 | 特点 |
|------|-------|----------|------|
| **不可分类** | pfifo_fast | 3优先级FIFO | 旧版默认，不可调参 |
| **不可分类** | fq_codel | 公平队列+CoDel | **新版默认**，解决Bufferbloat |
| **不可分类** | tbf | 令牌桶 | 简单限速 |
| **可分类** | htb | 分层令牌桶 | **推荐**，支持带宽借用和分类 |

**Bufferbloat问题**：过大的网络缓冲区导致数据包驻留时间超过超时阈值，引发全部请求超时失败，系统看似满载实则不可用。CoDel算法通过控制**驻留时间**而非队列长度来解决此问题。

#### 6.5.2 使用cgroup限制网络带宽

```bash
# 1. 创建cgroup并标记classid
mkdir /sys/fs/cgroup/net_cls/mygroup
echo 0x00010010 > /sys/fs/cgroup/net_cls/mygroup/net_cls.classid
# 0xAAAABBBB → AAAA:BBBB = 1:10

# 2. 配置HTB队列规则
tc qd add dev eth0 root handle 1: htb default 100
tc cl add dev eth0 parent 1: classid 1:1 htb rate 100mbit burst 20k
tc cl add dev eth0 parent 1:1 classid 1:10 htb rate 10mbit ceil 50mbit burst 20k
tc qd add dev eth0 parent 1:10 handle 10: fq_codel

# 3. 添加cgroup过滤器
tc fi add dev eth0 parent 1: protocol ip prio 1 handle 1: cgroup

# 4. 将进程加入cgroup
echo $PID > /sys/fs/cgroup/net_cls/mygroup/tasks
```

### 6.6 Cgroup V2（Unified Hierarchy）

Cgroup V2将所有资源控制器统一到单一层级，简化配置并新增重要功能：

**开启方法**：
```bash
# 内核引导参数
grubby --update-kernel=ALL --args=systemd.unified_cgroup_hierarchy=1
```

#### 6.6.1 V2核心变化

| 特性 | V1 | V2 |
|------|----|----|
| 层级结构 | 每个资源独立层级 | **统一层级** |
| Buffer IO限速 | ❌ 不支持 | ✅ **支持** |
| 压力通知 | eventfd（仅memory） | **PSI机制**（cpu/memory/io） |
| 线程级控制 | ❌ | ✅ threaded模式 |
| 配置复杂度 | 高 | **大幅简化** |

#### 6.6.2 V2配置示例

```bash
# 创建cgroup
mkdir /sys/fs/cgroup/mygroup

# 启用资源控制器
echo '+cpu +memory +io +pids' > /sys/fs/cgroup/cgroup.subtree_control

# CPU配额（max period格式）
echo "50000 100000" > /sys/fs/cgroup/mygroup/cpu.max      # 单核50%
echo "100" > /sys/fs/cgroup/mygroup/cpu.weight             # 权重（1-10000）

# 内存限制
echo "512M" > /sys/fs/cgroup/mygroup/memory.max            # 硬限制
echo "256M" > /sys/fs/cgroup/mygroup/memory.high           # 软限制（触发回收）
echo "128M" > /sys/fs/cgroup/mygroup/memory.min            # 硬保护（不被回收）
echo "1" > /sys/fs/cgroup/mygroup/memory.oom.group         # 整组OOM

# IO限速（支持Buffer IO！）
echo "253:0 wbps=2097152" > /sys/fs/cgroup/mygroup/io.max  # 写2MB/s
echo "253:0 rbps=10485760 wiops=100" > /sys/fs/cgroup/mygroup/io.max

# PIDS限制
echo "1000" > /sys/fs/cgroup/mygroup/pids.max

# 添加进程
echo $PID > /sys/fs/cgroup/mygroup/cgroup.procs
```

#### 6.6.3 PSI（Pressure Stall Information）

V2引入的**统一压力监控机制**，可监控cpu/memory/io的资源压力：

```bash
cat /sys/fs/cgroup/mygroup/cpu.pressure
# some avg10=4.56 avg60=2.30 avg300=1.15 total=234567
# full avg10=0.00 avg60=0.00 avg300=0.00 total=0

# some：至少有一个任务在等待资源的时间比例
# full：所有任务都在等待资源的时间比例（cpu无此项）
```

---

## 7. 系统调用与性能

### 7.1 系统调用开销

**用户态→内核态切换开销**：约100ns

```c
// strace统计系统调用
strace -c ./myapp
# % time  seconds  usecs/call  calls  errors  syscall
# 50.00   0.500000  50          10000   0      read
# 30.00   0.300000  30          10000   0      write
# 10.00   0.100000  100         1000    0      open
```

**减少系统调用**：
```go
// ❌ 频繁系统调用
for i := 0; i < 10000; i++ {
    syscall.Write(fd, []byte("x"))  // 10000次系统调用
}

// ✅ 批量写入
var buf bytes.Buffer
for i := 0; i < 10000; i++ {
    buf.WriteByte('x')
}
syscall.Write(fd, buf.Bytes())  // 1次系统调用
```

### 7.2 vDSO（虚拟动态共享对象）

**无需陷入内核的系统调用**：
```c
// gettimeofday()通过vDSO实现（无系统调用）
struct timeval tv;
gettimeofday(&tv, NULL);  // 直接读取内核在用户空间映射的时间页
```

**查看vDSO**：
```bash
ldd /bin/ls
# linux-vdso.so.1 => (0x00007ffe123ab000)  # vDSO
# libc.so.6 => /lib/x86_64-linux-gnu/libc.so.6
```

---

## 8. 面试题自查

### Q1: 进程和线程的本质区别是什么？什么时候用多进程，什么时候用多线程？

**答案**：
**本质区别**：
- **进程**：资源分配的基本单位，有独立的地址空间，进程间通信需要IPC
- **线程**：CPU调度的基本单位，共享进程地址空间，可直接访问全局变量

**使用场景**：
- **多进程**：需要强隔离（一个崩溃不影响其他）、利用多核、安全敏感
- **多线程**：需要共享大量数据、通信频繁、创建/销毁频繁

**Go语言建议**：使用Goroutine，它是轻量级线程，结合了多线程的易用性和多进程的安全性

---

### Q2: 什么是僵尸进程和孤儿进程？如何处理？

**答案**：
**僵尸进程**：
- 子进程退出后，父进程未调用wait()回收
- 占用PID资源，可能导致PID耗尽
- 处理：父进程调用wait()，或杀死父进程让init回收

**孤儿进程**：
- 父进程退出，子进程仍在运行
- 由init进程（PID=1）收养并回收
- 是正常现象，不会造成资源泄漏

---

### Q3: 虚拟内存有什么作用？页表是如何工作的？

**答案**：
**虚拟内存作用**：
1. **隔离**：每个进程独立地址空间，互不干扰
2. **扩展**：虚拟内存可大于物理内存（通过swap）
3. **共享**：多个进程共享同一物理页（如共享库）

**页表工作原理**：
- 虚拟地址通过多级页表（x86_64为4级）转换为物理地址
- TLB缓存常用的虚拟→物理映射，命中时无需查页表
- TLB Miss时触发Page Walk，遍历多级页表

---

### Q4: 什么是缺页中断？Minor和Major Page Fault有什么区别？

**答案**：
**缺页中断**：访问的虚拟页未映射到物理页时触发

**Minor Page Fault**：
- 页在内存中，但未映射到进程页表
- 只需更新页表，无需磁盘IO
- 常见于首次访问匿名映射

**Major Page Fault**：
- 页不在内存，需要从磁盘（swap或文件）读取
- 需要磁盘IO，开销大
- 应尽量避免（增加物理内存、减少swap使用）

---

### Q5: epoll和select有什么区别？为什么epoll更高效？

**答案**：
| 特性 | select | epoll |
|------|--------|-------|
| fd数量限制 | 1024 | 无限制 |
| 性能 | O(n)，遍历所有fd | O(1)，只遍历就绪fd |
| 内存拷贝 | 每次拷贝fd_set | 共享内存，无拷贝 |

**epoll高效原因**：
1. 使用红黑树管理fd，添加/删除O(logN)
2. 使用就绪链表，只返回就绪的fd
3. 使用mmap共享内存，避免拷贝

---

### Q6: 什么是零拷贝？有哪些实现方式？

**答案**：
**传统拷贝**：磁盘→内核→用户空间→Socket→网卡（4次拷贝，2次用户态/内核态切换）

**零拷贝实现**：
1. **sendfile**：磁盘→内核→网卡（2次拷贝），无需拷贝到用户空间
2. **mmap**：将文件映射到用户空间，避免read系统调用
3. **splice**：在两个fd之间直接传输数据，无需经过用户空间

**应用场景**：静态文件服务（Nginx）、Kafka消息传输

---

### Q7: 什么是伪共享（False Sharing）？如何避免？

**答案**：
**伪共享**：
- 多个线程访问不同变量，但变量在同一缓存行（64字节）
- 一个线程修改变量，导致其他线程的缓存行失效
- 严重影响多线程性能

**避免方法**：
1. 缓存行填充（Padding）：变量之间填充64字节
2. Go中使用独立的goroutine本地变量
3. 按缓存行对齐数据结构

---

### Q8: TCP三次握手的过程？为什么不是两次或四次？

**答案**：
**三次握手**：
1. 客户端→服务端：SYN（我要建立连接）
2. 服务端→客户端：SYN+ACK（同意，我也要建立连接）
3. 客户端→服务端：ACK（同意）

**为什么不是两次**：
- 防止旧的SYN包导致错误建立连接
- 确认双方都能收发

**为什么不是四次**：
- 第二步合并了SYN和ACK，没必要分开

---

### Q9: 什么是上下文切换？如何减少上下文切换？

**答案**：
**上下文切换**：
- 保存当前任务的CPU状态，恢复另一个任务的状态
- 进程切换：3-5μs（需要切换页表，TLB失效）
- 线程切换：1μs（同进程内，无需切换页表）

**减少方法**：
1. 使用协程（goroutine）替代线程
2. 减少锁竞争（无锁数据结构）
3. 使用IO多路复用（epoll）替代阻塞IO
4. 批量处理替代频繁调度

---

### Q10: Cgroup的CPU隔离有哪些方式？各有什么特点？

**答案**：
| 方式 | 机制 | 特点 |
|------|------|------|
| cpuset | 核心绑定 | 物理隔离，最强隔离性 |
| cpuquota | 时间配额 | 硬限制，不能超额使用 |
| cpushares | 权重比例 | 弹性限制，空闲时可借用 |

**选择建议**：
- 延迟敏感：cpuset（独占核心）
- 多租户硬隔离：cpuquota
- 弹性共享/超卖：cpushares

---

### Q11: Linux的IO调度算法有哪些？各适用什么场景？

**答案**：
| 算法 | 特点 | 适用场景 |
|------|------|----------|
| cfq | 完全公平，按进程分时间片 | 通用服务器 |
| deadline | 保证请求期限，防止饿死 | 数据库等IO密集型 |
| noop | 无调度，仅FIFO | SSD固态硬盘 |

**SSD为什么用noop**：
- SSD无寻道时间，不需要重排请求
- noop减少调度开销

---

### Q12: 什么是CFS调度器？nice值如何影响调度？

**答案**：
**CFS（完全公平调度器）**：
- Linux默认调度器
- 通过虚拟运行时间（vruntime）保证公平
- vruntime最小的进程优先调度

**nice值影响**：
- nice范围：-20（最高优先级）到19（最低优先级）
- nice值越小，权重越大，vruntime增长越慢
- nice=-10的进程获得的CPU时间约为nice=0的3倍

---

### Q13: 什么是内存映射（mmap）？有什么优势？

**答案**：
**mmap原理**：
- 将文件映射到进程的虚拟地址空间
- 访问内存即访问文件，无需read/write系统调用

**优势**：
1. 减少系统调用开销
2. 利用page cache，多进程共享
3. 支持随机访问（read需要顺序读取）

**应用场景**：数据库索引文件、大文件处理、进程间共享内存

---

### Q14: 什么是OOM Killer？如何避免被OOM Kill？

**答案**：
**OOM Killer**：
- 系统内存不足时，内核选择杀死进程释放内存
- 根据oom_score选择（分数越高越容易被杀）

**避免被杀**：
1. 设置oom_score_adj（-1000到1000，越小越不容易被杀）
2. 合理设置内存限制（Cgroup）
3. 监控内存使用，提前扩容
4. 使用swap缓冲（但会影响性能）

```bash
# 保护关键进程
echo -1000 > /proc/1234/oom_score_adj
```

---

### Q15: Go的GMP调度模型是什么？为什么这样设计？

**答案**：
**GMP模型**：
- **G**（Goroutine）：轻量级线程，2KB初始栈
- **M**（Machine）：内核线程，真正执行代码
- **P**（Processor）：逻辑处理器，数量=GOMAXPROCS

**为什么这样设计**：
1. 减少线程创建开销（复用M）
2. 工作窃取算法（P之间平衡负载）
3. 避免全局锁（每个P有本地队列）
4. 充分利用多核（P数量=核心数）

---

### Q16: 什么是Direct IO？什么时候应该使用？

**答案**：
**Direct IO**：
- 绕过page cache，直接读写磁盘
- 需要用户自己管理缓存

**使用场景**：
1. 数据库（自己管理buffer pool）
2. 大文件顺序读写（不需要缓存）
3. 应用级缓存更高效时

**注意事项**：
- 地址必须对齐（通常512字节）
- 大小必须是扇区的整数倍

---

### Q17: TCP拥塞控制是如何工作的？BBR和Cubic有什么区别？

**答案**：
**拥塞控制阶段**：
1. 慢启动：cwnd指数增长
2. 拥塞避免：cwnd线性增长
3. 快速重传：3个重复ACK立即重传
4. 快速恢复：cwnd减半继续传输

**Cubic vs BBR**：
- **Cubic**：基于丢包判断拥塞，保守
- **BBR**：基于带宽和RTT，主动探测，更激进

**BBR适用场景**：高带宽高延迟网络（如跨国链路）

---

### Q18: 如何排查内存泄漏？Go和C/C++分别用什么工具？

**答案**：
**Go内存泄漏排查**：
1. pprof查看heap：`go tool pprof http://localhost:6060/debug/pprof/heap`
2. 对比两次快照，找增长点
3. 常见原因：goroutine泄漏、全局变量持有引用

**C/C++内存泄漏排查**：
1. Valgrind：`valgrind --leak-check=full ./app`
2. AddressSanitizer（ASan）
3. 常见原因：malloc未free、循环引用

---

### Q19: 什么是信号（Signal）？如何优雅地处理SIGTERM？

**答案**：
**常用信号**：
- SIGTERM（15）：优雅退出请求
- SIGKILL（9）：强制杀死（不可捕获）
- SIGINT（2）：Ctrl+C中断
- SIGHUP（1）：终端断开

**优雅处理**：
```go
sigChan := make(chan os.Signal, 1)
signal.Notify(sigChan, syscall.SIGTERM, syscall.SIGINT)
go func() {
    <-sigChan
    // 1. 停止接收新请求
    // 2. 等待现有请求完成
    // 3. 关闭数据库连接
    // 4. 保存状态
    os.Exit(0)
}()
```

---

### Q20: 什么是Cgroup V2？相比V1有什么改进？

**答案**：
**主要改进**：
1. **统一层级**：所有资源控制器在单一层级，配置更简单
2. **Buffer IO限速**：V1不支持，V2支持
3. **PSI压力监控**：统一的cpu/memory/io压力指标
4. **线程级控制**：支持threaded模式

**V2核心文件**：
- `cpu.max`：CPU配额
- `memory.max`：内存硬限制
- `io.max`：IO限速
- `*.pressure`：PSI压力信息

---

## 总结

**操作系统核心知识点**：

1. **进程/线程**：调度（CFS/RT）、IPC（管道/共享内存/信号）、GMP模型、上下文切换
2. **内存**：虚拟内存、多级页表、TLB、缓存一致性、内存屏障、Buddy System、TCMalloc
3. **IO**：epoll vs select、零拷贝（sendfile/mmap）、Direct IO
4. **文件系统**：inode、page cache、fsync、ext4布局
5. **网络**：TCP握手、拥塞控制、Socket优化
6. **Cgroup资源隔离**：CPU（cpuset/cpuquota/cpushares）、内存（OOM/压力通知）、IO（blkio/调度算法）、网络（tc/HTB）、V2统一层级与PSI

**性能优化方向**：
- 减少系统调用（批量操作、vDSO）
- 减少内存拷贝（零拷贝）
- 利用page cache
- Cgroup资源隔离与限制
- 异步IO
- 避免伪共享


---
