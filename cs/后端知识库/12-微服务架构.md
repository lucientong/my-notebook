# 微服务架构

---

## 📑 目录

### 基础组件
1. [微服务基础](#1-微服务基础)
2. [服务注册与发现](#2-服务注册与发现)
3. [API网关](#3-api网关)
4. [服务熔断与降级](#4-服务熔断与降级)

### 治理能力
5. [分布式追踪](#5-分布式追踪)
6. [配置中心](#6-配置中心)
7. [分布式事务](#7-分布式事务)
8. [服务间通信](#8-服务间通信)

### 稳定性与自查
9. [限流算法](#9-限流算法)
10. [微服务测试](#10-微服务测试)
11. [面试题自查](#11-面试题自查)

---

## 1. 微服务基础

### 1.1 单体架构 vs 微服务架构

**单体架构**：

```
┌─────────────────────────────────┐
│         单体应用              │
│  ┌──────┬──────┬──────┬──────┐ │
│  │ 用户 │ 订单 │ 商品 │ 支付 │ │
│  └──────┴──────┴──────┴──────┘ │
│         ↓                     │
│      单一数据库               │
└─────────────────────────────────┘

优点：
- 开发简单（单一代码库）
- 部署简单（一个war/jar包）
- 调试简单（本地运行）

缺点：
- 难以扩展（必须整体扩展）
- 部署慢（一个模块改动，全部重新部署）
- 技术栈受限（必须用同一种语言/框架）
- 单点故障（一个模块崩溃，整个应用不可用）
```

**微服务架构**：

```
┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐
│ 用户服务│  │ 订单服务│  │ 商品服务│  │ 支付服务│
│   DB   │  │   DB   │  │   DB   │  │   DB   │
└────────┘  └────────┘  └────────┘  └────────┘
    ↓           ↓           ↓           ↓
         API网关（统一入口）
              ↓
          外部请求

优点：
- 独立部署（订单服务改动，不影响其他服务）
- 独立扩展（订单服务流量大，单独扩容）
- 技术异构（用户服务用Go，订单服务用Java）
- 故障隔离（订单服务崩溃，用户服务仍可用）

缺点：
- 运维复杂（服务数量多）
- 分布式事务（跨服务事务难处理）
- 服务间调用开销（网络延迟）
- 调试困难（请求跨多个服务）
```

---

### 1.2 微服务拆分原则

**按业务能力拆分**：

```
❌ 不推荐（按技术层拆分）：
- 用户Controller服务
- 用户Service服务
- 用户DAO服务

✅ 推荐（按业务能力拆分）：
- 用户服务（用户注册、登录、信息管理）
- 订单服务（创建订单、支付订单、订单查询）
- 商品服务（商品管理、库存管理）
```

**单一职责原则**：

```
✅ 每个服务只负责一个业务能力
❌ 避免"上帝服务"（OrderService包含支付、物流、库存...）
```

**高内聚、低耦合**：

```
✅ 服务内部高内聚（订单创建、支付、查询都在订单服务）
✅ 服务之间低耦合（订单服务调用支付服务通过API，不直接访问数据库）
```

---

## 2. 服务注册与发现

### 2.1 为什么需要服务注册与发现？

**传统方式（硬编码IP）**：

```go
// 订单服务调用用户服务
resp, err := http.Get("http://192.168.1.101:8080/user/123")

问题：
1. IP变化（扩容/缩容/故障）需要修改代码
2. 负载均衡需要自己实现
3. 健康检查需要自己实现
```

**服务注册与发现**：

```go
// 1. 用户服务启动时注册到注册中心
consul.Register("user-service", "192.168.1.101:8080")

// 2. 订单服务从注册中心获取用户服务地址
instances := consul.Discover("user-service")
// 返回：["192.168.1.101:8080", "192.168.1.102:8080", "192.168.1.103:8080"]

// 3. 负载均衡选择一个实例
instance := loadBalance(instances)
resp, err := http.Get(instance + "/user/123")

优势：
- 动态感知服务上下线
- 自动负载均衡
- 故障实例自动摘除
```

---

### 2.2 Consul原理⭐⭐⭐

**Consul架构**：

```
Consul集群（3-5个Server节点，Raft一致性）
    ↑
    │ 注册/发现
    ↓
Consul Agent（每个服务节点）
    ↑
    │ 健康检查
    ↓
微服务实例
```

**服务注册流程**：

```go
// 1. 服务启动时注册
registration := &consul.AgentServiceRegistration{
    ID:      "user-service-1",
    Name:    "user-service",
    Address: "192.168.1.101",
    Port:    8080,
    Check: &consul.AgentServiceCheck{
        HTTP:     "http://192.168.1.101:8080/health",
        Interval: "10s",  // 每10秒健康检查
        Timeout:  "3s",
    },
}
client.Agent().ServiceRegister(registration)

// 2. Consul Agent定期健康检查
// GET http://192.168.1.101:8080/health
// 200 OK → 健康
// 非200或超时 → 不健康（自动摘除）

// 3. 其他服务发现
services, _ := client.Health().Service("user-service", "", true, nil)
// 返回健康的实例列表
```

**为什么Consul用Raft一致性？**

```
场景：3个Consul Server节点

写入操作（服务注册）：
1. 客户端请求Leader
2. Leader写入log
3. Leader复制log到Follower（至少n/2+1节点确认）
4. Leader提交log，返回客户端
5. Leader通知Follower提交

为什么不用最终一致性（Gossip）？
- 服务注册需要强一致性（不能出现脑裂）
- Raft保证：只有一个Leader，数据不会冲突

为什么健康检查用Gossip？
- 健康状态可以容忍短暂不一致
- Gossip传播更快（O(log n)跳）
```

---

### 2.3 Etcd vs Consul vs Nacos

| 特性 | Etcd | Consul | Nacos |
|------|------|--------|-------|
| **一致性协议** | Raft | Raft | Raft+Distro |
| **健康检查** | 需要自己实现 | 内置（HTTP/TCP/脚本） | 内置 |
| **负载均衡** | 需要自己实现 | 内置 | 内置 |
| **多数据中心** | 不支持 | 支持 | 支持 |
| **UI界面** | 无 | 有 | 有 |
| **适用场景** | K8s元数据存储 | 微服务注册发现 | 微服务注册发现+配置中心 |

---

## 3. API网关

### 3.1 为什么需要API网关？

**无网关架构问题**：

```
客户端直接调用微服务：
移动端 → 用户服务
      → 订单服务
      → 商品服务

问题：
1. 客户端需要知道所有服务地址（耦合）
2. 每个服务都要实现认证、限流、日志（重复）
3. 跨域问题（CORS）
4. 协议转换（HTTP → gRPC）
```

**网关架构**：

```
客户端 → API网关 → 用户服务
                → 订单服务
                → 商品服务

网关统一处理：
- 认证鉴权
- 限流熔断
- 日志监控
- 协议转换
- 路由转发
```

---

### 3.2 Kong网关架构⭐⭐⭐

**Kong核心组件**：

```
客户端请求
    ↓
Kong Server（OpenResty + Lua）
    ↓
插件链（认证 → 限流 → 日志 → ...）
    ↓
Upstream（后端服务）
```

**Kong插件机制**：

```lua
-- 限流插件示例
function RateLimitPlugin:access(config)
  local current = redis:incr(key)
  
  if current > config.limit then
    return kong.response.exit(429, "Too Many Requests")
  end
  
  if current == 1 then
    redis:expire(key, config.period)
  end
end

-- 请求流程
请求 → 认证插件（JWT验证）
    → 限流插件（检查速率）
    → 日志插件（记录日志）
    → 转发到后端服务
```

**为什么Kong用OpenResty？**

```
OpenResty = Nginx + LuaJIT

优势：
1. 高性能（Nginx的事件驱动 + LuaJIT的JIT编译）
2. 灵活（Lua脚本动态扩展）
3. 生态丰富（大量Nginx插件）

性能：
- Kong：10万QPS（单机）
- Spring Cloud Gateway：5万QPS（单机）
```

---

### 3.3 网关路由策略

**基于路径路由**：

```yaml
# /api/user/* → 用户服务
# /api/order/* → 订单服务

routes:
  - path: /api/user/*
    service: user-service
  - path: /api/order/*
    service: order-service
```

**基于Header路由（灰度发布）**：

```yaml
# Header: X-Version=v2 → 新版本
# 其他 → 旧版本

routes:
  - path: /api/order/*
    match:
      headers:
        X-Version: v2
    service: order-service-v2
  - path: /api/order/*
    service: order-service-v1
```

---

## 4. 服务熔断与降级

### 4.1 为什么需要熔断？

**雪崩效应**：

```
用户服务 → 订单服务 → 商品服务（故障，响应慢5秒）
                    ↓
            订单服务线程池耗尽
                    ↓
            用户服务线程池耗尽
                    ↓
            整个系统不可用

熔断的作用：
- 快速失败（不等待5秒）
- 保护上游服务（防止雪崩）
- 给下游服务恢复时间
```

---

### 4.2 Hystrix熔断原理⭐⭐⭐

**熔断器三种状态**：

```
1. Closed（关闭）：
   - 正常请求通过
   - 记录失败率

2. Open（打开）：
   - 失败率 > 阈值（如50%）
   - 所有请求直接失败（快速失败）
   - 进入休眠期（如5秒）

3. Half-Open（半开）：
   - 休眠期后，允许少量请求通过（探测）
   - 成功 → Closed
   - 失败 → Open

状态转换：
Closed → Open（失败率>阈值）
Open → Half-Open（休眠期结束）
Half-Open → Closed（探测成功）
Half-Open → Open（探测失败）
```

**熔断器实现**：

```go
type CircuitBreaker struct {
    state         State  // Closed/Open/Half-Open
    failureCount  int
    successCount  int
    lastFailTime  time.Time
}

func (cb *CircuitBreaker) Call(fn func() error) error {
    if cb.state == Open {
        // 检查是否可以进入Half-Open
        if time.Since(cb.lastFailTime) > sleepWindow {
            cb.state = HalfOpen
        } else {
            return ErrCircuitOpen  // 快速失败
        }
    }
    
    err := fn()
    
    if err != nil {
        cb.onFailure()
    } else {
        cb.onSuccess()
    }
    
    return err
}

func (cb *CircuitBreaker) onFailure() {
    cb.failureCount++
    cb.lastFailTime = time.Now()
    
    if cb.state == HalfOpen {
        cb.state = Open  // 探测失败，重新打开
    } else if float64(cb.failureCount)/float64(cb.totalCount) > threshold {
        cb.state = Open  // 失败率超过阈值，打开
    }
}

func (cb *CircuitBreaker) onSuccess() {
    if cb.state == HalfOpen {
        cb.successCount++
        if cb.successCount >= successThreshold {
            cb.state = Closed  // 探测成功，关闭
            cb.failureCount = 0
        }
    }
}
```

**为什么熔断器能防止雪崩？**

```
无熔断器：
订单服务调用商品服务（5秒超时）
100个请求同时调用 → 100个线程阻塞5秒 → 线程池耗尽

有熔断器：
第1个请求：5秒超时（失败）
第2个请求：5秒超时（失败）
...
第10个请求：失败率>50% → 熔断器打开
第11-100个请求：立即失败（<1ms）→ 线程池未耗尽

节省时间：100 × 5秒 = 500秒 → 10 × 5秒 + 90 × 0.001秒 = 50秒
```

---

### 4.3 降级策略

**降级 vs 熔断**：

```
熔断：服务故障时，停止调用（快速失败）
降级：服务故障时，返回兜底数据（保证可用性）

示例：
// 熔断
err := orderService.GetOrder(id)
if err != nil {
    return err  // 直接返回错误
}

// 降级
err := orderService.GetOrder(id)
if err != nil {
    return defaultOrder  // 返回默认订单（兜底）
}
```

**降级策略**：

```
1. 返回默认值
   - 商品服务故障 → 返回空商品列表

2. 返回缓存数据
   - 订单详情服务故障 → 返回Redis缓存的订单

3. 降级到备用服务
   - 主支付服务故障 → 调用备用支付服务

4. 降级功能
   - 推荐服务故障 → 不展示推荐商品（不影响核心功能）
```

---

### 4.4 Sentinel vs Hystrix

| 特性 | Hystrix | Sentinel |
|------|---------|----------|
| **熔断粒度** | 线程池隔离 | 信号量隔离 |
| **实时监控** | 需要Dashboard | 内置实时监控 |
| **规则配置** | 代码配置 | 动态配置（控制台） |
| **性能** | 较低（线程切换） | 高（无线程切换） |
| **状态** | Netflix已停止维护 | 阿里维护中 |

**Sentinel优势**：

```
1. 实时监控：
   - 秒级监控（QPS、RT、成功率）
   - 可视化Dashboard

2. 动态规则：
   - 无需重启，动态修改限流规则
   - 规则持久化（Nacos/Apollo）

3. 丰富的降级策略：
   - 慢调用比例
   - 异常比例
   - 异常数
```

---

## 5. 分布式追踪

### 5.1 为什么需要分布式追踪？

**问题场景**：

```
用户请求 → 网关 → 用户服务 → 订单服务 → 商品服务
                              → 库存服务
                              → 支付服务

问题：
1. 请求慢，不知道哪个服务慢
2. 请求失败，不知道哪个服务失败
3. 调用链路复杂，难以调试
```

**分布式追踪**：

```
TraceID: abc123（唯一标识一个请求）
  ├─ SpanID: span1（网关，耗时10ms）
  ├─ SpanID: span2（用户服务，耗时20ms）
  └─ SpanID: span3（订单服务，耗时100ms）
      ├─ SpanID: span4（商品服务，耗时50ms）
      ├─ SpanID: span5（库存服务，耗时30ms）
      └─ SpanID: span6（支付服务，耗时20ms）

分析：
- 总耗时130ms（10+20+100）
- 瓶颈：订单服务（100ms）
  - 其中商品服务最慢（50ms）
```

---

### 5.2 OpenTelemetry原理⭐⭐⭐

**核心概念**：

```
1. Trace：一次完整的请求链路（TraceID唯一）
2. Span：一个操作（SpanID唯一）
3. Context：上下文传播（TraceID + SpanID）

数据结构：
Trace {
    TraceID: "abc123"
    Spans: [
        Span {
            SpanID: "span1"
            ParentSpanID: ""  // 根Span
            Name: "GET /api/order"
            StartTime: 1234567890
            EndTime:   1234567900
            Attributes: {
                http.method: "GET"
                http.status_code: 200
            }
        },
        Span {
            SpanID: "span2"
            ParentSpanID: "span1"  // 父Span
            Name: "OrderService.GetOrder"
            ...
        }
    ]
}
```

**上下文传播（Context Propagation）**：

```
请求流：
客户端 → 网关 → 订单服务 → 商品服务

步骤1：客户端生成TraceID
TraceID = "abc123"
SpanID = "span1"

步骤2：网关接收请求，提取TraceID
从Header提取：Traceparent: 00-abc123-span1-01
创建新Span：SpanID = "span2"，ParentSpanID = "span1"

步骤3：网关调用订单服务，传递TraceID
Header: Traceparent: 00-abc123-span2-01

步骤4：订单服务接收请求，提取TraceID
创建新Span：SpanID = "span3"，ParentSpanID = "span2"

步骤5：订单服务调用商品服务，传递TraceID
Header: Traceparent: 00-abc123-span3-01

...

最终：所有Span都有相同的TraceID，可以串联起来
```

**Traceparent格式（W3C标准）**：

```
Traceparent: 00-abc123-span1-01
             ││  │      │     │
             ││  │      │     └─ flags（是否采样）
             ││  │      └─ parent-id（父SpanID）
             ││  └─ trace-id（TraceID）
             │└─ version（版本）
             └─ 00（固定）
```

---

### 5.3 Jaeger架构

**组件**：

```
应用 → Jaeger Agent（本地） → Jaeger Collector → 存储（Elasticsearch）
                                                 ↓
                                            Jaeger UI
```

**采样策略**：

```
1. 全量采样（const sampler）：
   - 采样率100%
   - 适合开发环境

2. 概率采样（probabilistic sampler）：
   - 采样率1%（100个请求采样1个）
   - 适合高流量生产环境

3. 限速采样（rate limiting sampler）：
   - 每秒最多采样10个
   - 避免采样数据过多

4. 自适应采样（adaptive sampler）：
   - 根据流量动态调整采样率
   - 保证采样数据量稳定
```

---

## 6. 配置中心

### 6.1 为什么需要配置中心？

**传统配置方式问题**：

```
配置写在application.yml：
- 修改配置需要重启服务
- 多个服务配置重复（数据库地址）
- 配置散落各处，难以管理
```

**配置中心**：

```
Apollo/Nacos：
- 集中管理配置
- 动态更新配置（无需重启）
- 配置版本管理（回滚）
- 配置权限管理
```

---

### 6.2 Apollo架构

**组件**：

```
1. Config Service：
   - 提供配置查询接口
   - 长轮询推送配置变更

2. Admin Service：
   - 配置管理接口
   - Portal调用

3. Portal：
   - 配置管理界面

4. Client：
   - 应用内的SDK
   - 长轮询获取配置
```

**配置热更新原理**：

```
步骤1：应用启动，Client拉取配置
GET /configs?appId=order-service&namespace=application

步骤2：Client启动长轮询
GET /notifications/v2?appId=order-service&notifications=[...]
（Config Service hold住请求，60秒超时）

步骤3：运维在Portal修改配置
Portal → Admin Service → Config Service

步骤4：Config Service立即返回长轮询请求
返回：{namespace: "application", notificationId: 2}

步骤5：Client收到通知，拉取新配置
GET /configs?appId=order-service&namespace=application

步骤6：Client触发回调
@ApolloConfigChangeListener
public void onChange(ConfigChangeEvent event) {
    String newValue = event.getChange("timeout").getNewValue();
    // 更新本地配置（无需重启）
}
```

---

## 7. 分布式事务⭐⭐⭐

### 7.1 分布式事务的挑战

**为什么微服务需要分布式事务？**

```
单体架构：
用户服务、订单服务、库存服务 → 同一个数据库
@Transactional → 本地事务保证ACID

微服务架构：
用户服务 → 用户库
订单服务 → 订单库
库存服务 → 库存库
无法使用本地事务！

典型场景：下单
1. 订单服务：创建订单
2. 库存服务：扣减库存
3. 账户服务：扣减余额

如果库存扣减成功，但余额扣减失败？
- 数据不一致：订单创建、库存扣了，但钱没扣
```

---

### 7.2 Saga模式

**正向操作 + 补偿操作**：

```
Saga工作流程：
T1 → T2 → T3 → 成功
       ↓
      失败
       ↓
C2 ← C1（补偿）

T: Transaction（正向操作）
C: Compensation（补偿操作）

示例：下单流程
T1: 创建订单（状态=待支付）  | C1: 取消订单
T2: 扣减库存                | C2: 恢复库存  
T3: 扣减余额                | C3: 恢复余额

场景1：全部成功
T1 → T2 → T3 → 订单完成

场景2：T3失败（余额不足）
T1 → T2 → T3(失败) → C2 → C1
创建订单 → 扣库存 → 扣余额失败 → 恢复库存 → 取消订单
```

**Saga编排方式**：

```
1. 编排式（Choreography）：
   - 事件驱动，服务间通过消息通信
   - 订单服务 → 发布"订单创建"事件
   - 库存服务 → 监听事件，扣减库存，发布"库存扣减"事件
   - 优点：松耦合
   - 缺点：流程难追踪，复杂时难以管理

2. 协调式（Orchestration）：
   - 有一个中心协调器（Saga Orchestrator）
   - 协调器依次调用各服务
   - 优点：流程清晰，易于管理
   - 缺点：协调器是单点

推荐：简单流程用编排式，复杂流程用协调式
```

---

### 7.3 TCC模式

**Try-Confirm-Cancel**：

```
三个阶段：
1. Try：资源预留（不真正执行）
2. Confirm：确认执行（真正扣减）
3. Cancel：取消预留（回滚）

示例：扣减余额100元
Try：
  UPDATE account SET frozen = frozen + 100 WHERE balance >= 100
  （冻结100元，但balance不变）

Confirm：
  UPDATE account SET balance = balance - 100, frozen = frozen - 100
  （真正扣减balance，解除冻结）

Cancel：
  UPDATE account SET frozen = frozen - 100
  （解除冻结，回滚）

TCC vs Saga：
- TCC：资源预留，隔离性更好
- Saga：无预留，可能出现"脏读"
```

**TCC适用场景**：

```
- 资金类业务（扣款、转账）
- 需要强隔离性
- 性能要求高（无锁等待）

难点：
- 每个服务都要实现Try/Confirm/Cancel三个接口
- 业务侵入性强
```

---

### 7.4 本地消息表

**可靠消息最终一致性**：

```
步骤1：本地事务写入业务数据 + 消息表
BEGIN;
  INSERT INTO orders (id, status) VALUES (1, 'created');
  INSERT INTO message_table (id, content, status) VALUES (1, '订单1创建', 'pending');
COMMIT;

步骤2：后台任务定时扫描消息表
SELECT * FROM message_table WHERE status = 'pending';

步骤3：发送消息到MQ
producer.send(message);
UPDATE message_table SET status = 'sent' WHERE id = 1;

步骤4：消费者消费消息
库存服务：扣减库存
consumer.ack();

步骤5：收到确认，更新消息状态
UPDATE message_table SET status = 'consumed' WHERE id = 1;

优点：
- 无分布式事务协调器
- 依赖MQ的可靠投递

缺点：
- 消息延迟
- 需要定时任务
```

---

### 7.5 最大努力通知

```
适用场景：
- 对一致性要求不高
- 允许重试通知

示例：支付结果通知
1. 用户支付成功
2. 支付系统通知商户系统
3. 商户系统返回成功 → 结束
   商户系统返回失败或超时 → 重试（最多N次）

重试策略：
- 第1次：立即
- 第2次：1分钟后
- 第3次：5分钟后
- ...
- 超过重试次数：人工介入
```

---

## 8. 服务间通信⭐⭐

### 8.1 同步通信 vs 异步通信

```
同步通信（REST/gRPC）：
订单服务 ─调用→ 库存服务 ─响应→ 订单服务
优点：实时性高，调用链清晰
缺点：服务耦合，调用方阻塞

异步通信（消息队列）：
订单服务 ─发消息→ MQ ─消费→ 库存服务
优点：解耦，削峰
缺点：复杂度高，最终一致性

选择依据：
- 需要立即响应：同步
- 可以容忍延迟：异步
- 调用链长：异步（避免阻塞）
```

---

### 8.2 gRPC vs REST

```
特性对比：
| 特性       | REST          | gRPC              |
|------------|---------------|-------------------|
| 协议       | HTTP/1.1      | HTTP/2            |
| 序列化     | JSON          | Protobuf（二进制）|
| 性能       | 较低          | 高                |
| 浏览器支持 | 好            | 需要grpc-web      |
| 调试       | 简单（可读）  | 复杂（二进制）    |

gRPC优势：
1. 高性能：Protobuf序列化 + HTTP/2多路复用
2. 强类型：protobuf定义接口，编译时检查
3. 流式传输：支持双向流

适用场景：
- 内部服务通信：gRPC（高性能）
- 对外API：REST（兼容性好）
```

---

### 8.3 服务版本管理

```
API版本策略：
1. URL路径版本：
   /api/v1/orders
   /api/v2/orders

2. Header版本：
   Accept: application/vnd.api.v1+json
   Accept: application/vnd.api.v2+json

3. 查询参数版本：
   /api/orders?version=1

向后兼容原则：
- 新增字段：可以（老客户端忽略）
- 删除字段：不可以（老客户端依赖）
- 修改字段类型：不可以（破坏兼容性）

版本迁移：
1. 发布v2版本，v1继续运行
2. 通知客户端迁移到v2
3. v1流量降到0后，下线v1
```

---

## 9. 限流算法⭐⭐⭐

### 9.1 固定窗口计数器

```
原理：
- 将时间划分为固定窗口（如1秒）
- 每个窗口内计数
- 超过阈值则拒绝

实现：
┌─────────┐ ┌─────────┐ ┌─────────┐
│ 0-1秒   │ │ 1-2秒   │ │ 2-3秒   │
│ 计数:80 │ │ 计数:60 │ │ 计数:?  │
│ 限制:100│ │ 限制:100│ │ 限制:100│
└─────────┘ └─────────┘ └─────────┘

问题：边界突刺
- 0.9-1.0秒：100个请求
- 1.0-1.1秒：100个请求
- 0.2秒内200个请求（超过限制）
```

---

### 9.2 滑动窗口

```
原理：
- 窗口滑动，而不是跳变
- 细分为多个小窗口

实现：
时间线：|----|----|----|----|
       0  0.25 0.5 0.75  1秒

滑动窗口（0.5-1.5秒）：
- 0.5-0.75秒：20个请求
- 0.75-1.0秒：30个请求
- 1.0-1.25秒：25个请求
- 总计：75个请求 < 100，通过

优点：
- 解决边界突刺问题
- 更平滑的限流

Redis实现：
ZADD rate_limiter <timestamp> <request_id>
ZREMRANGEBYSCORE rate_limiter 0 <current_time - window_size>
ZCARD rate_limiter
```

---

### 9.3 令牌桶

```
原理：
- 桶中有令牌，每个请求消耗一个令牌
- 令牌以固定速率添加
- 桶满则丢弃新令牌

特点：
- 允许突发流量（桶中有令牌就通过）
- 但平均速率受限（令牌添加速率）

实现：
tokens = min(capacity, tokens + (now - lastRefill) * rate)
if tokens >= 1:
    tokens -= 1
    allow()
else:
    reject()

参数：
- capacity: 桶容量（决定突发量）
- rate: 令牌添加速率（决定平均速率）

适用场景：
- 允许短时突发，但限制平均速率
- 如：限制API每秒100请求，但允许短时200请求
```

---

### 9.4 漏桶

```
原理：
- 请求进入桶
- 请求以固定速率流出
- 桶满则拒绝新请求

特点：
- 强制恒定速率（不允许突发）
- 平滑输出

对比：
令牌桶：允许突发，控制平均速率
漏桶：强制恒定速率，不允许突发

适用场景：
- 对下游有严格速率限制
- 如：调用第三方API，限制每秒10次
```

---

## 10. 微服务测试⭐⭐

### 10.1 测试金字塔

```
              △
             /  \
            / E2E \  ← 端到端测试（少量）
           /--------\
          /  集成    \  ← 集成测试（中等）
         /--------------\
        /      单元      \  ← 单元测试（大量）
       /------------------\

原则：
- 单元测试最多（快速、低成本）
- E2E测试最少（慢、高成本）
```

---

### 10.2 契约测试（Contract Testing）

```
问题：
订单服务依赖库存服务
- 库存服务修改了响应格式
- 订单服务不知道，解析失败

契约测试：
1. 消费者（订单服务）定义契约：
   我期望：GET /inventory/123 返回 {"productId": 123, "quantity": 100}

2. 提供者（库存服务）验证契约：
   运行契约测试，确保响应满足消费者期望

3. CI/CD流程：
   库存服务部署前，验证所有消费者契约

工具：
- Pact（HTTP契约测试）
- Spring Cloud Contract

优点：
- 早期发现集成问题
- 无需部署所有服务
```

---

### 10.3 混沌工程

```
原则：
- 在生产环境注入故障
- 验证系统的弹性

常见故障注入：
1. 网络延迟：服务调用延迟增加
2. 网络丢包：部分请求失败
3. 服务宕机：某个服务不可用
4. CPU/内存压力：资源耗尽

工具：
- Chaos Monkey（Netflix）：随机杀死实例
- Chaos Mesh（K8s）：K8s环境混沌工程
- Litmus（CNCF）：声明式混沌实验

示例：
实验：库存服务延迟5秒
预期：订单服务熔断，返回降级响应
验证：监控错误率、响应时间、熔断器状态
```

---

## 11. 面试题自查

### Q1: 微服务和单体架构的本质区别是什么？什么时候应该使用微服务？

**答案**：
**本质区别**：
- **部署单元**：单体是一个可部署单元，微服务是多个独立可部署单元
- **数据库**：单体共享数据库，微服务每个服务有独立数据库
- **团队组织**：单体按技术层划分（前端组、后端组），微服务按业务能力划分（订单组、用户组）

**适用场景**：
- 单体：小团队、业务简单、快速迭代初期
- 微服务：大团队、业务复杂、需要独立扩展和部署

---

### Q2: 微服务如何拆分？有什么原则？

**答案**：
**拆分原则**：
1. **按业务能力拆分**：用户服务、订单服务、支付服务（而非按技术层拆分）
2. **单一职责**：每个服务只负责一个业务能力
3. **高内聚低耦合**：服务内部功能紧密相关，服务之间依赖最小化

**拆分方法**：
- DDD领域驱动设计：识别限界上下文（Bounded Context）
- 事件风暴：通过业务事件识别服务边界
- 康威定律：按团队结构拆分

**避免**：
- 过度拆分（纳米服务）
- 拆分后数据库仍然共享

---

### Q3: 服务注册与发现的实现原理？为什么Consul用Raft协议？

**答案**：
**实现原理**：
1. **服务注册**：服务启动时向注册中心注册（IP、端口、健康检查URL）
2. **健康检查**：注册中心定期检查服务状态，不健康实例自动摘除
3. **服务发现**：调用方从注册中心获取可用实例列表
4. **负载均衡**：客户端根据策略（轮询、随机、最小连接）选择实例

**Raft协议原因**：
- 服务注册需要**强一致性**（不能出现脑裂）
- Raft保证：只有一个Leader处理写入，数据不会冲突
- 健康检查用Gossip（允许短暂不一致，传播更快）

---

### Q4: API网关有什么作用？Kong为什么性能高？

**答案**：
**网关作用**：
1. **统一入口**：客户端只需知道网关地址
2. **认证鉴权**：JWT验证、OAuth2.0
3. **限流熔断**：保护后端服务
4. **协议转换**：HTTP→gRPC
5. **路由转发**：根据路径/Header转发到不同服务

**Kong高性能原因**：
- 基于OpenResty（Nginx + LuaJIT）
- Nginx的事件驱动模型 + LuaJIT的JIT编译
- 单机可达10万QPS（Spring Cloud Gateway约5万QPS）

---

### Q5: 熔断器的三种状态是什么？如何防止雪崩效应？

**答案**：
**三种状态**：
1. **Closed（关闭）**：正常请求通过，记录失败率
2. **Open（打开）**：失败率超阈值，所有请求直接失败（快速失败）
3. **Half-Open（半开）**：休眠期后，允许少量请求探测，成功则关闭，失败则打开

**防止雪崩**：
- 无熔断：100个请求等待5秒超时 → 500秒线程阻塞 → 线程池耗尽 → 级联故障
- 有熔断：前10个失败后熔断打开 → 后90个立即失败（<1ms）→ 线程池保护

---

### Q6: Saga和TCC分布式事务有什么区别？各自适用什么场景？

**答案**：
**Saga模式**：
- 正向操作 + 补偿操作（T1→T2→T3，失败则C2→C1）
- 无资源预留，可能出现"脏读"
- 适用：长事务、最终一致性可接受

**TCC模式**：
- Try（预留）+ Confirm（确认）+ Cancel（取消）
- 有资源预留，隔离性更好
- 适用：资金业务、强隔离性要求

**选择建议**：
- 一般业务：Saga（简单）
- 金融业务：TCC（可靠）

---

### Q7: 本地消息表如何实现可靠消息？

**答案**：
**实现步骤**：
1. 本地事务同时写入业务表和消息表
2. 后台任务定时扫描待发送消息
3. 发送到消息队列，更新消息状态为"已发送"
4. 消费者消费成功后，更新消息状态为"已消费"

**核心点**：
- 业务操作和消息写入在同一个本地事务中
- 利用MQ的可靠投递保证最终一致性
- 消费者需要幂等（消息可能重复）

---

### Q8: 分布式追踪是如何实现的？TraceID如何传递？

**答案**：
**核心概念**：
- **Trace**：一次完整请求的调用链路
- **Span**：一个操作单元（一次RPC调用）
- **Context**：上下文传播（TraceID + ParentSpanID）

**TraceID传递**：
1. 入口服务生成TraceID和SpanID
2. 调用下游服务时，通过HTTP Header传递：`Traceparent: 00-{TraceID}-{SpanID}-01`
3. 下游服务提取TraceID，创建新Span，ParentSpanID指向调用方SpanID
4. 所有Span上报到Collector，根据TraceID和ParentSpanID重建调用链

---

### Q9: 配置中心如何实现配置热更新？

**答案**：
**Apollo热更新原理**：
1. 客户端启动时拉取配置
2. 客户端启动长轮询（60秒超时）
3. 配置变更时，服务端立即返回长轮询请求
4. 客户端收到通知，拉取新配置
5. 触发本地回调，应用新配置（无需重启）

**关键技术**：
- 长轮询（Long Polling）：比轮询实时，比WebSocket简单
- 配置回调：应用通过监听器感知配置变化

---

### Q10: gRPC和REST有什么区别？各自适用什么场景？

**答案**：
| 特性 | REST | gRPC |
|------|------|------|
| 协议 | HTTP/1.1 | HTTP/2（多路复用） |
| 序列化 | JSON（文本） | Protobuf（二进制） |
| 性能 | 较低 | 高（10倍以上） |
| 浏览器支持 | 原生支持 | 需要grpc-web |

**适用场景**：
- **gRPC**：内部服务通信（高性能、强类型）
- **REST**：对外API（兼容性好、易调试）

---

### Q11: 限流算法有哪些？令牌桶和漏桶有什么区别？

**答案**：
**四种限流算法**：
1. **固定窗口**：按时间窗口计数，有边界突刺问题
2. **滑动窗口**：细分时间窗口，更平滑
3. **令牌桶**：令牌固定速率添加，允许突发
4. **漏桶**：请求固定速率流出，强制恒定速率

**令牌桶vs漏桶**：
- 令牌桶：允许突发流量（桶中有令牌就通过），控制平均速率
- 漏桶：强制恒定速率，不允许突发

**使用场景**：
- 令牌桶：用户请求限流（允许短时突发）
- 漏桶：调用第三方API（严格速率限制）

---

### Q12: Sentinel和Hystrix有什么区别？为什么推荐Sentinel？

**答案**：
| 特性 | Hystrix | Sentinel |
|------|---------|----------|
| 隔离方式 | 线程池隔离 | 信号量隔离 |
| 性能 | 较低（线程切换） | 高 |
| 规则配置 | 代码配置 | 动态配置（控制台） |
| 监控 | 需要Dashboard | 内置实时监控 |
| 维护状态 | Netflix已停止维护 | 阿里维护中 |

**Sentinel优势**：
- 实时监控：秒级QPS、RT、成功率监控
- 动态规则：无需重启，动态修改限流规则
- 丰富策略：慢调用比例、异常比例、异常数

---

### Q13: 如何保证微服务API的向后兼容性？

**答案**：
**版本策略**：
- URL路径版本：`/api/v1/orders`、`/api/v2/orders`
- Header版本：`Accept: application/vnd.api.v1+json`

**兼容性原则**：
- ✅ 新增字段：老客户端忽略新字段
- ❌ 删除字段：老客户端依赖该字段
- ❌ 修改字段类型：破坏序列化/反序列化

**版本迁移**：
1. 发布v2版本，v1继续运行
2. 通知客户端迁移，设置deadline
3. v1流量降到0后下线

---

### Q14: 什么是契约测试？为什么微服务需要契约测试？

**答案**：
**问题场景**：
- 库存服务修改响应格式，订单服务不知道，解析失败
- 只有E2E测试才能发现，成本高、发现晚

**契约测试**：
1. 消费者定义契约：期望的请求和响应格式
2. 提供者验证契约：确保满足消费者期望
3. CI/CD中自动验证：提供者部署前验证所有契约

**优点**：
- 早期发现集成问题（不需要部署所有服务）
- 防止提供者破坏消费者

---

### Q15: 什么是混沌工程？如何在微服务中实践？

**答案**：
**定义**：在生产环境主动注入故障，验证系统弹性

**常见故障注入**：
1. 网络延迟：服务调用延迟增加
2. 网络丢包：部分请求失败
3. 服务宕机：随机杀死实例
4. 资源耗尽：CPU/内存压力

**实践步骤**：
1. 定义稳态指标（QPS、错误率、P99延迟）
2. 假设系统能承受某种故障
3. 注入故障，观察指标变化
4. 分析结果，修复问题

**工具**：Chaos Monkey（Netflix）、Chaos Mesh（K8s）

---

### Q16: 微服务如何实现灰度发布？

**答案**：
**方式**：
1. **网关路由**：根据Header/Cookie将部分流量路由到新版本
2. **负载均衡**：权重调整（90%旧版本，10%新版本）
3. **特征标记**：根据用户ID/地域/VIP等级分流

**流程**：
1. 部署新版本（不接收流量）
2. 灰度1%流量，观察错误率和延迟
3. 逐步扩大：5% → 10% → 25% → 50% → 100%
4. 每阶段观察监控，有问题立即回滚

**关键点**：
- 监控指标：错误率、P99延迟、业务指标
- 快速回滚能力

---

### Q17: 服务间同步调用和异步调用各适用什么场景？

**答案**：
**同步调用（REST/gRPC）**：
- 需要立即获取结果
- 调用链短
- 例：查询用户信息

**异步调用（消息队列）**：
- 可以容忍延迟
- 需要解耦
- 例：下单后发送短信、异步更新统计

**选择原则**：
- 读操作：同步
- 写操作（可延迟）：异步
- 调用链长：异步（避免阻塞）
- 流量削峰：异步

---

### Q18: 微服务中如何处理跨服务的数据查询？

**答案**：
**方案**：
1. **API组合**：网关/BFF调用多个服务，聚合结果
2. **数据冗余**：订单服务冗余用户信息（冗余更新要一致）
3. **CQRS**：读写分离，查询服务维护聚合视图
4. **事件驱动**：服务发布事件，其他服务同步数据

**最佳实践**：
- 简单查询：API组合
- 复杂查询：CQRS + 事件驱动
- 避免跨库JOIN

---

### Q19: 微服务如何实现幂等性？

**答案**：
**幂等性定义**：同一个请求执行一次和多次效果相同

**实现方案**：
1. **唯一请求ID**：客户端生成RequestID，服务端去重
2. **数据库唯一约束**：利用唯一索引防止重复插入
3. **乐观锁**：带版本号更新，版本不匹配则拒绝
4. **状态机**：订单状态只能单向流转

**代码示例**：
```go
// 1. 检查请求是否已处理
if redis.Get(requestId) != nil {
    return cachedResult
}
// 2. 处理请求
result := process(request)
// 3. 缓存结果
redis.SetEx(requestId, result, 24*time.Hour)
return result
```

---

### Q20: 微服务架构有哪些常见的反模式？

**答案**：
**常见反模式**：
1. **分布式单体**：服务拆分了但数据库共享，仍然强耦合
2. **纳米服务**：过度拆分，一个功能拆成多个服务
3. **共享库地狱**：通过共享库耦合，修改库影响所有服务
4. **同步调用链过长**：A→B→C→D→E，延迟累加，任一故障导致全链路失败
5. **缺少熔断**：下游故障导致上游雪崩
6. **忽略数据一致性**：跨服务事务未处理

**解决方案**：
- 数据库隔离
- 合理拆分粒度
- 异步解耦
- 熔断降级
- 分布式事务（Saga/TCC）

---

## 总结

微服务核心组件：
1. **服务注册与发现**：Consul、Etcd、Nacos
2. **API网关**：Kong、Spring Cloud Gateway
3. **服务熔断**：Hystrix、Sentinel
4. **分布式追踪**：Jaeger、Zipkin、SkyWalking
5. **配置中心**：Apollo、Nacos
6. **分布式事务**：Saga、TCC、本地消息表


---