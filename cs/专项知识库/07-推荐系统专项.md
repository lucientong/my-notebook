# 推荐系统专项

> **定位**：从系统架构视角全面讲解工业级推荐系统的设计与实现，涵盖召回、粗排、精排、重排全链路，特征工程、AB 实验、在线服务等工程实践。
>
> **前置建议**：`09-系统设计与架构`（高可用/限流/缓存）、`07-Redis与缓存`（特征缓存）、`08-消息队列`（实时特征流）

---

## 📑 目录

- [1. 推荐系统整体架构](#1-推荐系统整体架构)
  - [1.1 推荐系统在产品中的位置](#11-推荐系统在产品中的位置)
  - [1.2 漏斗模型：召回→粗排→精排→重排](#12-漏斗模型召回粗排精排重排)
  - [1.3 数据流与系统模块全景](#13-数据流与系统模块全景)
- [2. 召回层](#2-召回层)
  - [2.1 多路召回架构](#21-多路召回架构)
  - [2.2 协同过滤召回](#22-协同过滤召回)
  - [2.3 向量化召回（双塔模型）](#23-向量化召回双塔模型)
  - [2.4 图召回与实时召回](#24-图召回与实时召回)
  - [2.5 召回层工程实践](#25-召回层工程实践)
- [3. 粗排层](#3-粗排层)
  - [3.1 粗排的定位与必要性](#31-粗排的定位与必要性)
  - [3.2 知识蒸馏与轻量模型](#32-知识蒸馏与轻量模型)
  - [3.3 粗排的工程优化](#33-粗排的工程优化)
- [4. 精排层](#4-精排层)
  - [4.1 特征工程](#41-特征工程)
  - [4.2 经典模型演进](#42-经典模型演进)
  - [4.3 多目标学习](#43-多目标学习)
  - [4.4 模型在线推理](#44-模型在线推理)
- [5. 重排层](#5-重排层)
  - [5.1 重排的核心目标](#51-重排的核心目标)
  - [5.2 多样性算法](#52-多样性算法)
  - [5.3 业务策略与规则引擎](#53-业务策略与规则引擎)
- [6. 特征系统](#6-特征系统)
  - [6.1 特征分类体系](#61-特征分类体系)
  - [6.2 特征存储与服务](#62-特征存储与服务)
  - [6.3 实时特征工程](#63-实时特征工程)
- [7. 样本构造与训练流水线](#7-样本构造与训练流水线)
  - [7.1 正负样本定义](#71-正负样本定义)
  - [7.2 样本偏差与纠偏](#72-样本偏差与纠偏)
  - [7.3 训练流水线架构](#73-训练流水线架构)
- [8. AB 实验平台](#8-ab-实验平台)
  - [8.1 分流与分桶](#81-分流与分桶)
  - [8.2 核心指标体系](#82-核心指标体系)
  - [8.3 实验分析方法](#83-实验分析方法)
- [9. 在线服务架构](#9-在线服务架构)
  - [9.1 推荐服务整体架构](#91-推荐服务整体架构)
  - [9.2 低延迟优化策略](#92-低延迟优化策略)
  - [9.3 稳定性保障](#93-稳定性保障)
- [10. 冷启动与 Exploration](#10-冷启动与-exploration)
- [11. 面试题自查](#11-面试题自查)

---

## 1. 推荐系统整体架构

### 1.1 推荐系统在产品中的位置

```
┌─────────────────────────────────────────────────────┐
│                    用户端产品                         │
│                                                     │
│  搜索框 ─→ 搜索系统                                  │
│  信息流 ─→ 推荐系统  ←── 本文重点                     │
│  广告位 ─→ 广告系统（与推荐类似但优化目标为 eCPM）      │
│                                                     │
│  推荐系统核心使命：                                    │
│  在海量内容中，为每个用户找到其最感兴趣的 N 条内容       │
│  并以最优顺序展示                                     │
└─────────────────────────────────────────────────────┘
```

**推荐 vs 搜索**：

| 维度 | 推荐 | 搜索 |
|------|------|------|
| 用户意图 | 隐式（行为推断） | 显式（query 表达） |
| 候选空间 | 全量物品 | query 相关物品 |
| 核心指标 | 时长/CTR/留存 | 相关性/满足率 |
| 个性化程度 | 高（千人千面） | 中（同 query 结果相近） |

### 1.2 漏斗模型：召回→粗排→精排→重排

```
┌─────────────────────────────────────────────────────────────────┐
│                     推荐系统漏斗架构                              │
│                                                                 │
│  全量物品池                                                      │
│  │  ~10^7 - 10^9 量级                                           │
│  ▼                                                              │
│  ┌──────────────────┐                                           │
│  │  召回层 (Recall)  │  多路召回：协同过滤/双塔/热门/实时         │
│  │  ~10^4 候选       │  延迟要求：< 50ms                        │
│  │  简单模型/规则     │  目标：高召回率（不遗漏好内容）            │
│  └────────┬─────────┘                                           │
│           ▼                                                     │
│  ┌──────────────────┐                                           │
│  │  粗排层 (Pre-Rank)│  轻量模型/知识蒸馏                        │
│  │  ~10^3 候选       │  延迟要求：< 20ms                        │
│  │  简化版精排模型    │  目标：粗粒度筛选                         │
│  └────────┬─────────┘                                           │
│           ▼                                                     │
│  ┌──────────────────┐                                           │
│  │  精排层 (Rank)    │  复杂深度模型（DeepFM/DIN/多目标）        │
│  │  ~10^2 候选       │  延迟要求：< 30ms                        │
│  │  完整特征 + 交叉   │  目标：精准预估（CTR/CVR/时长）          │
│  └────────┬─────────┘                                           │
│           ▼                                                     │
│  ┌──────────────────┐                                           │
│  │  重排层 (Re-Rank) │  多样性/打散/业务规则/上下文               │
│  │  最终 N 条展示    │  延迟要求：< 5ms                          │
│  │  规则 + 策略引擎   │  目标：用户体验（多样性/新鲜度）          │
│  └──────────────────┘                                           │
└─────────────────────────────────────────────────────────────────┘
```

### 1.3 数据流与系统模块全景

```
┌──────────────────────────────────────────────────────────────────┐
│                      推荐系统数据流全景                            │
│                                                                  │
│  用户行为日志 ──→ 实时流处理 (Flink) ──→ 实时特征 ──→ 特征服务    │
│       │                │                                ↑        │
│       │                ▼                                │        │
│       │          样本拼接 ──→ 训练数据 ──→ 模型训练      │        │
│       │                                    │            │        │
│       │                                    ▼            │        │
│       │                              模型发布 ──→ 模型服务       │
│       │                                                  ↑       │
│       ▼                                                  │       │
│  离线批处理 (Spark) ──→ 离线特征 ──→ 特征服务 ──→ 推荐引擎       │
│       │                                          │               │
│       └──→ 物品画像 ──→ 物品索引 ──→ 召回服务 ──→ │               │
│                                                  ▼               │
│                                          AB 实验分流              │
│                                                  │               │
│                                                  ▼               │
│                                           返回推荐结果            │
└──────────────────────────────────────────────────────────────────┘
```

---

## 2. 召回层

### 2.1 多路召回架构

```
┌─────────── 多路召回 ───────────┐
│                                │
│  ① 协同过滤召回 ──→ 200 条     │
│  ② 双塔向量召回 ──→ 500 条     │
│  ③ 热门/趋势召回 ──→ 100 条    │
│  ④ 实时兴趣召回 ──→ 200 条     │
│  ⑤ 图召回 ────────→ 200 条     │
│  ⑥ 地理位置召回 ──→ 100 条     │
│                                │
│  合并去重 ──→ ~1,000 条候选     │
└────────────────────────────────┘
```

**多路召回的设计理念**：
- 每路召回有不同的偏好建模能力（协同过滤关注行为相似，向量召回关注语义相似）
- 多路互补，提高整体覆盖率
- 每路独立，可以并行调用，故障隔离

### 2.2 协同过滤召回

#### ItemCF (物品协同过滤)

```
原理：用户喜欢物品 A，找到与 A 相似的物品推荐

物品相似度计算：
sim(i, j) = |Users(i) ∩ Users(j)| / sqrt(|Users(i)| × |Users(j)|)

改进（IUF — 热门用户降权）：
sim(i, j) = Σ_{u ∈ Users(i) ∩ Users(j)} 1/log(1 + |Items(u)|)
             / sqrt(|Users(i)| × |Users(j)|)
```

```python
# ItemCF 离线计算（Spark 伪代码）
def compute_item_similarity(user_items):
    """
    输入：user_id -> [item_id, ...]
    输出：(item_i, item_j) -> similarity
    """
    # 1. 构建倒排索引：item -> [user, ...]
    item_users = user_items.flatMap(
        lambda (uid, items): [(item, uid) for item in items]
    ).groupByKey()

    # 2. 对每对共现物品计算相似度
    # 3. 取 top-K 相似物品存入物品相似度表
```

#### UserCF (用户协同过滤)

```
原理：找到与目标用户兴趣相似的用户，推荐他们喜欢的物品

适用场景：社交类产品（好友推荐）、用户少物品多的场景
```

**ItemCF vs UserCF**：

| 维度 | ItemCF | UserCF |
|------|--------|--------|
| 适用场景 | 物品少用户多（电商） | 用户少物品多（新闻） |
| 实时性 | 物品相似度稳定 | 需跟踪用户兴趣变化 |
| 长尾覆盖 | 较好 | 一般 |
| 可解释性 | "因为你看了X" | "和你相似的人在看" |
| 冷启动 | 新物品冷启动困难 | 新用户冷启动困难 |

### 2.3 向量化召回（双塔模型）

```
┌──────── 双塔模型 (Two-Tower) ────────┐
│                                      │
│  用户塔          物品塔               │
│  ┌─────┐        ┌─────┐             │
│  │User │        │Item │             │
│  │Features      │Features            │
│  │  ↓  │        │  ↓  │             │
│  │ DNN │        │ DNN │             │
│  │  ↓  │        │  ↓  │             │
│  │User │        │Item │             │
│  │Embed│        │Embed│             │
│  └──┬──┘        └──┬──┘             │
│     │              │                 │
│     └──── cos(u,i) ─┘               │
│         或 inner product             │
│                                      │
│  离线：物品塔 → 向量 → 构建 ANN 索引  │
│  在线：用户塔 → 向量 → ANN 检索 TopK  │
└──────────────────────────────────────┘
```

**ANN (Approximate Nearest Neighbor) 检索**：

| 算法 | 原理 | 适用场景 |
|------|------|---------|
| **Faiss IVF** | 倒排量化聚类 | 大规模、可 GPU 加速 |
| **HNSW** | 分层可导航小世界图 | 高精度、内存充足 |
| **ScaNN** | Google 提出的量化方法 | 高吞吐 |
| **Milvus** | 向量数据库 | 生产级、支持增量更新 |

> 📖 交叉引用：`10-数据库综合` — Milvus 向量数据库的详细讲解

```python
# Faiss 索引构建示例
import faiss
import numpy as np

dim = 128
n_items = 1_000_000
item_vectors = np.random.randn(n_items, dim).astype('float32')

# IVF + PQ 量化索引
nlist = 1024  # 聚类中心数
m = 16        # PQ 子量化器数
quantizer = faiss.IndexFlatIP(dim)
index = faiss.IndexIVFPQ(quantizer, dim, nlist, m, 8)
index.train(item_vectors)
index.add(item_vectors)

# 在线检索
user_vector = np.random.randn(1, dim).astype('float32')
index.nprobe = 64  # 搜索的聚类中心数
scores, ids = index.search(user_vector, k=500)  # 检索 top-500
```

### 2.4 图召回与实时召回

#### 图召回

```
用户-物品二部图上的游走：

User A ──click──→ Item 1 ──click_by──→ User B ──click──→ Item 5
                                                         ↑
                                                   推荐给 User A

算法：
- ItemCF on Graph: 基于共现关系的图上随机游走
- DeepWalk / Node2Vec: 图 Embedding 学习
- GraphSAGE: 归纳式图神经网络
```

#### 实时召回

```
┌──────── 实时兴趣召回 ────────┐
│                              │
│  用户最近 30 分钟的行为序列：  │
│  click(A) → click(B) → C    │
│                              │
│  实时 Trigger → 物品相似表   │
│  A → [A1, A2, A3]           │
│  B → [B1, B2, B3]           │
│  合并 → 实时候选集            │
│                              │
│  特点：捕捉短期兴趣变化       │
│  实现：读取实时行为 + 查表    │
└──────────────────────────────┘
```

### 2.5 召回层工程实践

**召回质量评估**：

| 指标 | 定义 | 目标 |
|------|------|------|
| Recall@K | 用户真正交互的物品中，被召回的比例 | > 80% |
| HitRate@K | 至少召回一个正样本的用户比例 | > 95% |
| 覆盖率 | 被召回的物品占全量物品的比例 | > 30% |
| 新鲜度 | 召回物品中新发布物品的比例 | 视业务而定 |

**工程要点**：
- 各路召回**并行调用**，设超时兜底
- 召回结果**去重** + **过滤**（已曝光/违规/过期）
- 召回候选数量可通过 AB 实验动态调整

---

## 3. 粗排层

### 3.1 粗排的定位与必要性

```
为什么需要粗排？

召回层：~10,000 条候选，模型简单（向量内积）
精排层：~数百条候选，模型复杂（DeepFM/DIN + 完整特征）

问题：精排模型计算 10,000 条太慢（每条 ~0.5ms × 10,000 = 5s）
方案：在中间加粗排层，用轻量模型将 10,000 筛选到 1,000

粗排 = "精排模型的简化版"
- 使用精排模型的子集特征
- 或使用知识蒸馏训练的轻量模型
```

### 3.2 知识蒸馏与轻量模型

```
┌──────── 知识蒸馏 (Knowledge Distillation) ────────┐
│                                                    │
│  Teacher Model (精排大模型)                         │
│  ├── 输入：完整特征                                 │
│  └── 输出：soft label (概率分布)                    │
│              ↓                                     │
│  Student Model (粗排小模型)                         │
│  ├── 输入：简化特征                                 │
│  ├── 目标：拟合 Teacher 的 soft label               │
│  └── Loss = α × CE(y, p_s) + (1-α) × KL(p_t, p_s) │
│                                                    │
│  效果：Student 学到 Teacher 的"暗知识"              │
│  例如：Teacher 知道物品 A 和 B 的相关性，即使        │
│  Student 没有看到 AB 的交叉特征                     │
└────────────────────────────────────────────────────┘
```

### 3.3 粗排的工程优化

| 优化策略 | 方法 |
|---------|------|
| 特征裁剪 | 只用 user profile + item 基础特征，去掉交叉特征 |
| 模型简化 | 浅层 DNN (2-3层) 或 LR + 简单交叉 |
| 向量预计算 | 物品侧 Embedding 离线算好，在线只算用户侧 |
| 批量推理 | 将 1,000 条 batch 送入 GPU 一次性推理 |
| 量化 | INT8 量化减少计算量 |

---

## 4. 精排层

> 📖 交叉引用：`AI知识库/05-推荐模型与训练` — 各模型的详细原理与训练技巧

### 4.1 特征工程

```
┌──────── 特征分类 ────────────────────────────────┐
│                                                   │
│  用户侧特征：                                      │
│  ├── 基础画像：性别、年龄、城市、注册天数             │
│  ├── 统计特征：7日/30日 点击率、观看时长              │
│  ├── 行为序列：最近 50 次点击的物品 ID 序列          │
│  └── 实时特征：最近 30min 点击/停留行为              │
│                                                   │
│  物品侧特征：                                      │
│  ├── 基础属性：类目、标签、作者、发布时间             │
│  ├── 统计特征：CTR、完播率、转发率                   │
│  └── 内容特征：标题 Embedding、封面 Embedding        │
│                                                   │
│  交叉特征：                                        │
│  ├── 用户-物品交叉：用户对该类目的偏好度              │
│  ├── 上下文：时间（早/午/晚）、设备、网络             │
│  └── 实时交叉：用户最近是否浏览过同类目               │
└───────────────────────────────────────────────────┘
```

**特征处理**：

| 特征类型 | 处理方式 |
|---------|---------|
| 连续值（年龄、价格） | 分桶离散化 / 标准化 / log 变换 |
| 离散值（城市、类目） | Embedding 查表 |
| 序列特征（行为序列） | DIN Attention / Transformer |
| 多值特征（标签列表） | Sum/Mean Pooling |
| 文本特征 | 预训练模型 → Embedding |

### 4.2 经典模型演进

```
LR → FM → DeepFM → DIN → DIEN → Transformer
│     │     │        │     │       │
│     │     │        │     │       └── 序列建模 + 注意力
│     │     │        │     └── 兴趣演化：GRU + AUGRU
│     │     │        └── 注意力机制：对行为序列加权
│     │     └── 深度 + 二阶交叉
│     └── 二阶特征交叉
└── 线性模型

时间线：2010 ──── 2016 ──── 2017 ──── 2018 ──── 2019 ──── 2020+
```

**各模型核心思想**：

| 模型 | 核心创新 | 适用场景 |
|------|---------|---------|
| **LR** | 线性可解释 | 基线/简单场景 |
| **FM** | 二阶特征交叉 `<vi, vj>·xi·xj` | 稀疏特征交叉 |
| **DeepFM** | FM + DNN 并行 | 低阶+高阶交叉 |
| **Wide&Deep** | Memorization + Generalization | Google Play 推荐 |
| **DCN** | Cross Network 显式高阶交叉 | 自动特征交叉 |
| **DIN** | Attention 加权用户历史行为 | 用户行为序列建模 |
| **DIEN** | GRU 建模兴趣演化 | 兴趣动态变化 |
| **BST** | Transformer 替代 GRU | 长序列建模 |
| **MMOE** | 多专家+门控 | 多目标学习 |

### 4.3 多目标学习

```
┌──────── 多目标模型架构 (MMOE) ────────┐
│                                       │
│  共享底层 → 多个 Expert Network        │
│              ↓                         │
│         Gate Network (per task)        │
│              ↓                         │
│  ┌─────────┬──────────┬────────────┐  │
│  │ CTR Tower│CVR Tower │ 时长 Tower │  │
│  │ P(click) │P(convert)│ E(duration)│  │
│  └─────────┴──────────┴────────────┘  │
│                                       │
│  最终排序分 = f(pCTR, pCVR, E_dur)    │
│  例如：score = pCTR × (α×pCVR + β×dur)│
└───────────────────────────────────────┘
```

**多目标融合策略**：

| 策略 | 公式 | 特点 |
|------|------|------|
| 加权求和 | `w1*pCTR + w2*pCVR + w3*dur` | 简单，权重需调参 |
| 乘法模型 | `pCTR × (a*pCVR + b*dur + c)` | 常用，CTR 作为门控 |
| 排序学习 | LambdaRank / ListNet | 端到端优化列表指标 |
| 约束优化 | 满足 CTR 约束下最大化时长 | 复杂但灵活 |

### 4.4 模型在线推理

```
┌──────── 在线推理架构 ────────┐
│                              │
│  推荐引擎                     │
│    │                         │
│    ├── 特征拼接               │
│    │   ├── 查询 User 特征    │
│    │   ├── 查询 Item 特征    │
│    │   └── 计算交叉特征       │
│    │                         │
│    ├── 模型推理               │
│    │   ├── TF Serving        │
│    │   ├── TensorRT          │
│    │   └── ONNX Runtime      │
│    │                         │
│    └── 后处理                 │
│        ├── 分数校准           │
│        └── 业务过滤           │
└──────────────────────────────┘
```

**推理延迟优化**：

| 策略 | 效果 | 说明 |
|------|------|------|
| Batch 推理 | 减少 GPU 调用次数 | 将多条候选打包一次推理 |
| INT8 量化 | 2-3x 加速 | TensorRT / ONNX |
| 算子融合 | 减少内存拷贝 | Attention + LayerNorm 融合 |
| 特征预计算 | 减少在线计算 | 物品 Embedding 离线存储 |
| GPU 推理 | 并行计算 | NVIDIA Triton Server |

---

## 5. 重排层

### 5.1 重排的核心目标

精排给出的是每个物品的独立评分，但**最终展示是一个列表**，需要考虑：

1. **多样性**：避免连续展示同类内容（如连续 5 条美食视频）
2. **新鲜度**：给新发布内容曝光机会
3. **公平性**：避免头部物品垄断流量
4. **业务规则**：运营置顶、广告插入、合规过滤
5. **上下文适配**：考虑已展示内容的影响

### 5.2 多样性算法

#### MMR (Maximal Marginal Relevance)

```
MMR = argmax_{d ∈ R\S} [λ × Rel(d) - (1-λ) × max_{d' ∈ S} Sim(d, d')]

其中：
- R: 候选集
- S: 已选集
- Rel(d): 物品 d 的相关性分数（精排分）
- Sim(d, d'): 物品间的相似度
- λ: 相关性与多样性的权衡参数

贪心选择：每次选一个 MMR 最大的物品加入结果
```

#### 类目打散规则

```python
def category_spread(ranked_list, max_consecutive=2):
    """
    规则：同类目物品最多连续出现 max_consecutive 次
    """
    result = []
    waiting = []  # 被延后的物品

    for item in ranked_list:
        recent_cats = [r.category for r in result[-max_consecutive:]]
        if len(recent_cats) == max_consecutive and all(c == item.category for c in recent_cats):
            waiting.append(item)  # 延后
        else:
            result.append(item)

    # 将 waiting 中的物品插入合适位置
    for item in waiting:
        insert_pos = find_best_position(result, item)
        result.insert(insert_pos, item)

    return result
```

### 5.3 业务策略与规则引擎

```
┌──────── 重排策略引擎 ────────┐
│                              │
│  规则优先级（从高到低）：      │
│                              │
│  ① 合规过滤（违规内容移除）   │
│  ② 运营置顶（强制置顶N条）   │
│  ③ 已曝光过滤（去重）        │
│  ④ 广告混排（插入广告位）     │
│  ⑤ 多样性打散               │
│  ⑥ 新内容 Boost             │
│  ⑦ 冷启动物品 Explore 位     │
│                              │
│  实现：规则引擎 + 配置中心    │
│  可热更新，无需重新部署       │
└──────────────────────────────┘
```

---

## 6. 特征系统

> 📖 交叉引用：`07-Redis与缓存`（特征缓存）、`14-大数据处理基础`（Flink/Spark 处理）

### 6.1 特征分类体系

```
┌──────────────────────────────────────────────────────┐
│                特征按时效性分类                         │
│                                                      │
│  离线特征 (T+1 更新)                                  │
│  ├── 用户长期画像（性别/年龄/城市）                     │
│  ├── 物品统计（历史 CTR/完播率）                       │
│  └── 协同过滤相似表                                   │
│  存储：HBase / Redis / Feature Store                  │
│  更新：每日 Spark 批量计算                             │
│                                                      │
│  近线特征 (分钟级更新)                                 │
│  ├── 最近 1 小时用户行为统计                           │
│  ├── 物品实时热度                                     │
│  └── 用户实时兴趣标签                                 │
│  存储：Redis / Flink State                            │
│  更新：Flink 流计算窗口聚合                            │
│                                                      │
│  实时特征 (请求级计算)                                 │
│  ├── 上下文特征（时间/设备/网络）                       │
│  ├── 用户-物品交叉（用户对此类目的偏好）                │
│  └── 列表上下文（已展示物品的影响）                     │
│  计算：在线推荐引擎实时计算                             │
└──────────────────────────────────────────────────────┘
```

### 6.2 特征存储与服务

```
┌──────── Feature Store 架构 ────────┐
│                                    │
│  特征注册 (Schema)                  │
│  ├── 特征名、类型、维度             │
│  ├── 更新频率、数据源               │
│  └── 版本管理                       │
│                                    │
│  离线存储层                         │
│  ├── Hive/HDFS (历史全量)           │
│  └── 训练样本拼接                   │
│                                    │
│  在线存储层                         │
│  ├── Redis Cluster (热特征)         │
│  ├── Tair/Pika (大容量特征)         │
│  └── 本地 LRU Cache (超热特征)      │
│                                    │
│  特征服务 API                       │
│  ├── BatchGet(entity_id, features) │
│  ├── 多级缓存：本地 → Redis → DB    │
│  └── 降级策略：超时返回默认值        │
└────────────────────────────────────┘
```

### 6.3 实时特征工程

```python
# Flink 实时特征计算示例（伪代码）

# 1. 用户实时 CTR（近 1 小时滑动窗口）
user_clicks = (
    click_stream
    .key_by("user_id")
    .window(SlidingEventTimeWindows.of(Time.hours(1), Time.minutes(5)))
    .aggregate(ClickCountAggregator())
)

user_impressions = (
    impression_stream
    .key_by("user_id")
    .window(SlidingEventTimeWindows.of(Time.hours(1), Time.minutes(5)))
    .aggregate(ImpressionCountAggregator())
)

realtime_ctr = (
    user_clicks.join(user_impressions)
    .where("user_id").equal_to("user_id")
    .window(TumblingEventTimeWindows.of(Time.minutes(5)))
    .apply(lambda clicks, imps: clicks / max(imps, 1))
)

# 2. 物品实时热度
item_hot_score = (
    click_stream
    .key_by("item_id")
    .window(SlidingEventTimeWindows.of(Time.minutes(30), Time.minutes(1)))
    .aggregate(WeightedClickAggregator())  # 近期权重更高
)

# 3. 写入 Redis
realtime_ctr.add_sink(RedisSink("user_realtime_ctr"))
item_hot_score.add_sink(RedisSink("item_hot_score"))
```

---

## 7. 样本构造与训练流水线

### 7.1 正负样本定义

| 场景 | 正样本 | 负样本 |
|------|--------|--------|
| CTR 预估 | 用户点击 | 曝光未点击 |
| CVR 预估 | 点击后转化 | 点击未转化 |
| 观看时长 | 观看 > 阈值 | 观看 < 阈值 / 回归值 |
| 视频完播 | 播放 > 80% | 播放 < 30% |

### 7.2 样本偏差与纠偏

```
┌──────── 推荐系统的样本偏差 ────────────┐
│                                       │
│  ① 位置偏差 (Position Bias)            │
│     位置靠前的物品获得更多点击           │
│     纠偏：位置作为特征 / IPW 加权        │
│                                       │
│  ② 选择偏差 (Selection Bias)           │
│     只有被推荐的物品才有反馈             │
│     纠偏：随机流量探索 / 反事实学习       │
│                                       │
│  ③ 流行度偏差 (Popularity Bias)        │
│     热门物品获得更多曝光和点击           │
│     纠偏：降采样 / 逆倾向加权            │
│                                       │
│  ④ 曝光偏差 (Exposure Bias)            │
│     用户只能对看到的物品反馈             │
│     纠偏：负样本采样策略                 │
└───────────────────────────────────────┘
```

**负样本采样策略**：

| 策略 | 方法 | 适用 |
|------|------|------|
| 全量负样本 | 曝光未点击全部作负样本 | 精排（候选少） |
| 随机负采样 | 全量物品中随机采样作负样本 | 召回（候选多） |
| Hard Negative | 曝光未点击但排名靠前的 | 提升模型区分度 |
| 热门负采样 | 按热度加权采样 | 去除流行度偏差 |

### 7.3 训练流水线架构

```
┌──────── 模型训练流水线 ────────────────────────┐
│                                                │
│  ① 样本拼接 (Join)                              │
│     行为日志 + 用户特征 + 物品特征 → 训练样本    │
│     工具：Spark / Flink (离线/实时)              │
│                                                │
│  ② 样本存储                                     │
│     格式：TFRecord / Parquet                    │
│     存储：HDFS / S3                             │
│     周期：T+1 全量 + 小时级增量                  │
│                                                │
│  ③ 模型训练                                     │
│     框架：TensorFlow / PyTorch                  │
│     分布式：参数服务器 / AllReduce               │
│     加速：GPU 集群 + 混合精度                    │
│                                                │
│  ④ 模型验证                                     │
│     离线指标：AUC / GAUC / NDCG                 │
│     在线 AB：CTR / 时长 / 留存                   │
│                                                │
│  ⑤ 模型上线                                     │
│     导出 SavedModel → TF Serving                │
│     灰度发布 → 全量                              │
│                                                │
│  ⑥ 在线学习 (Online Learning)                   │
│     实时样本 → 增量更新模型参数                   │
│     周期：分钟级                                 │
│     风险：需要监控模型质量，异常自动回滚           │
└────────────────────────────────────────────────┘
```

---

## 8. AB 实验平台

### 8.1 分流与分桶

```
┌──────── AB 实验分层分流 ────────┐
│                                │
│  Layer 1: 召回实验              │
│  ├── Bucket 1-50:  新召回策略   │
│  └── Bucket 51-100: 对照组      │
│                                │
│  Layer 2: 精排实验              │
│  ├── Bucket 1-30:  新模型 V2    │
│  ├── Bucket 31-60: 新特征实验   │
│  └── Bucket 61-100: 对照组      │
│                                │
│  Layer 3: 重排实验              │
│  ├── Bucket 1-50:  新打散策略   │
│  └── Bucket 51-100: 对照组      │
│                                │
│  分流方式：hash(user_id + layer_id) % 100
│  关键：层间正交，层内互斥        │
└────────────────────────────────┘
```

### 8.2 核心指标体系

| 类型 | 指标 | 定义 | 关注方 |
|------|------|------|--------|
| 效率指标 | CTR | 点击数/曝光数 | 算法 |
| 效率指标 | CVR | 转化数/点击数 | 算法 |
| 用户体验 | 人均使用时长 | 总时长/DAU | 产品 |
| 用户体验 | 次日留存率 | 次日回访/今日活跃 | 产品 |
| 内容生态 | 内容覆盖率 | 被曝光内容/总内容 | 运营 |
| 内容生态 | 创作者活跃 | 有分发的创作者占比 | 运营 |
| 护栏指标 | 负反馈率 | 举报+不感兴趣/曝光 | 质量 |
| 护栏指标 | 多样性 | 曝光类目数的 entropy | 体验 |

### 8.3 实验分析方法

| 方法 | 说明 | 适用场景 |
|------|------|---------|
| **T-test** | 检验两组均值差异的显著性 | 基础 AB 测试 |
| **Delta Method** | 处理比率指标的方差估计 | CTR/CVR 等比率指标 |
| **CUPED** | 使用实验前数据降低方差 | 加速实验收敛 |
| **DID** | 双重差分，排除时间趋势 | 新老用户分析 |
| **多重假设校正** | Bonferroni/BH | 同时看多个指标 |

---

## 9. 在线服务架构

### 9.1 推荐服务整体架构

```
┌──────── 在线推荐服务架构 ──────────────────────┐
│                                                │
│  客户端请求                                     │
│      ↓                                         │
│  API Gateway (Nginx/Envoy)                     │
│      ↓                                         │
│  推荐主服务 (Recommend Service)                 │
│  ├── AB 分流模块                                │
│  ├── 召回编排器 (并行调用多路召回)               │
│  │   ├── CF Service (gRPC)                     │
│  │   ├── ANN Service (gRPC)                    │
│  │   ├── Hot Service (gRPC)                    │
│  │   └── Realtime Service (gRPC)               │
│  ├── 粗排服务 (Rank-Lite)                       │
│  ├── 特征服务 (Feature Service)                 │
│  │   └── Redis Cluster + Local Cache           │
│  ├── 精排服务 (Rank Service)                    │
│  │   └── TF Serving / Triton (GPU)             │
│  ├── 重排模块 (Re-Rank)                         │
│  └── 日志收集 (Kafka → HDFS)                    │
│      ↓                                         │
│  返回推荐列表                                    │
└────────────────────────────────────────────────┘
```

### 9.2 低延迟优化策略

| 策略 | 细节 | 效果 |
|------|------|------|
| **并行调用** | 多路召回并行、特征批量获取 | 减少串行等待 |
| **超时截断** | 每个下游设置超时，超时用兜底 | 保证 p99 延迟 |
| **本地缓存** | 热特征/热模型结果 LRU 缓存 | 减少 RPC 调用 |
| **预计算** | 物品 Embedding 离线算好 | 减少在线计算 |
| **连接池** | gRPC 长连接复用 | 减少连接开销 |
| **异步日志** | 日志异步写 Kafka，不阻塞主流程 | 减少同步 IO |
| **结果缓存** | 同一用户短时间内复用上次结果 | 减少重复计算 |

### 9.3 稳定性保障

> 📖 交叉引用：`09-系统设计与架构`（熔断降级）、`专项知识库/04-SRE高级实践`（可观测性）

```
┌──────── 推荐系统稳定性保障体系 ────────┐
│                                       │
│  降级策略：                             │
│  ├── L1: 召回超时 → 使用缓存召回结果   │
│  ├── L2: 精排超时 → 降级为粗排分       │
│  ├── L3: 特征缺失 → 使用默认值填充     │
│  └── L4: 全链路超时 → 返回热门兜底列表  │
│                                       │
│  监控告警：                             │
│  ├── 延迟: p50/p99/p999               │
│  ├── 成功率: 各服务调用成功率           │
│  ├── 质量: CTR/时长等业务指标实时监控   │
│  └── 资源: CPU/内存/GPU 利用率         │
│                                       │
│  容灾：                                │
│  ├── 多机房部署                        │
│  ├── 模型灰度发布                      │
│  └── 异常模型自动回滚                  │
└───────────────────────────────────────┘
```

---

## 10. 冷启动与 Exploration

### 新用户冷启动

```
┌──────── 新用户冷启动策略 ────────┐
│                                 │
│  阶段 1: 注册时                  │
│  ├── 基于注册渠道/设备推断       │
│  └── 展示热门+多样化内容         │
│                                 │
│  阶段 2: 前 N 次交互             │
│  ├── 兴趣引导（让用户选标签）    │
│  ├── 增加 Explore 流量比例       │
│  └── 快速学习初始兴趣            │
│                                 │
│  阶段 3: 有一定行为后             │
│  ├── 逐步过渡到个性化推荐        │
│  └── 降低 Explore 比例           │
└─────────────────────────────────┘
```

### 新物品冷启动

| 策略 | 方法 |
|------|------|
| 内容理解 | 基于标题/封面/标签生成物品 Embedding |
| Look-alike | 找到相似的已有物品，复用其用户群 |
| 流量扶持 | 给新物品保底曝光量 |
| 快速反馈 | 小流量探索，根据实时 CTR 调整曝光 |

### Exploration vs Exploitation

```
┌──────── E&E 策略 ────────────────┐
│                                  │
│  ε-greedy：                      │
│  以 ε 概率随机探索，1-ε 概率利用  │
│  简单但不够高效                   │
│                                  │
│  UCB (Upper Confidence Bound)：  │
│  score = mean + c × sqrt(ln(N)/n)│
│  探索被选次数少的物品              │
│                                  │
│  Thompson Sampling：              │
│  为每个物品维护 Beta 分布          │
│  采样选择（不确定性越大越可能被选） │
│  工业界常用                       │
│                                  │
│  Contextual Bandit：              │
│  结合用户上下文的 Bandit           │
│  LinUCB / Neural Bandit           │
└──────────────────────────────────┘
```

---

## 11. 面试题自查

### Q1：推荐系统的漏斗模型是什么？为什么需要多层？

**答**：漏斗模型将推荐分为**召回→粗排→精排→重排**四层：
- **召回**：从亿级物品池中快速筛选万级候选（多路并行，简单模型）
- **粗排**：用轻量模型将万级减到千级（知识蒸馏/简化精排）
- **精排**：用复杂深度模型对千级候选精准打分（完整特征+交叉）
- **重排**：考虑多样性/业务规则，生成最终展示列表

多层的原因是**精度与效率的 tradeoff**：精排模型太复杂无法对全量物品打分，召回模型太简单无法精准排序。

---

### Q2：双塔模型的优缺点？如何做向量召回？

**答**：
**优点**：
- 用户塔和物品塔解耦，物品向量可离线计算
- 在线只需计算用户向量 + ANN 检索，延迟低
- 可利用海量曝光数据训练

**缺点**：
- 用户和物品只在最后一层交互（内积/余弦），交叉能力弱
- 无法利用交叉特征

**向量召回流程**：
1. 离线：物品塔 → 计算全量物品向量 → 构建 ANN 索引（如 HNSW/IVF）
2. 在线：用户塔 → 计算用户向量 → ANN 检索 Top-K

---

### Q3：精排模型从 LR 到 DIN 的演进逻辑？

**答**：
- **LR**：线性模型，需手工构造交叉特征
- **FM**：自动学习二阶特征交叉 `<vi,vj>·xi·xj`
- **DeepFM**：FM（低阶交叉）+ DNN（高阶交叉）并行
- **DIN**：引入注意力机制，对用户历史行为**按候选物品加权**（"和候选物品相关的行为权重更高"）
- **DIEN**：在 DIN 基础上用 GRU 建模兴趣的**时序演化**

核心趋势：从手工特征 → 自动交叉 → 序列建模 → 注意力机制

---

### Q4：推荐系统有哪些样本偏差？怎么处理？

**答**：
1. **位置偏差**：靠前位置 CTR 天然更高 → 训练时将位置作为特征，预测时设为无偏位置
2. **选择偏差**：只有被推荐的物品才有反馈 → 随机流量探索/反事实学习
3. **流行度偏差**：热门物品曝光多 → 负样本按热度加权采样/降采样热门
4. **曝光偏差**：未曝光物品无反馈 → 负样本不等于不喜欢，用较低权重

---

### Q5：如何设计推荐系统的 AB 实验？如何判断实验效果？

**答**：
**实验设计**：
- 分层分桶：按 `hash(user_id + layer_id) % 100` 分流
- 层间正交（不同层的实验可以叠加），层内互斥
- 对照组保持不变

**效果判断**：
- 核心指标：CTR、时长、留存等
- 统计显著性：T-test / Delta Method，p < 0.05
- CUPED 降方差加速实验
- 护栏指标：检查负反馈率等不恶化
- 建议观察 7 天以上，排除新鲜感效应

---

### Q6：推荐系统在线服务如何保证低延迟？

**答**：
1. **并行化**：多路召回并行调用，特征批量获取
2. **超时机制**：每个 RPC 设超时，超时走降级
3. **缓存层级**：热特征 Local Cache → Redis → DB 三级缓存
4. **预计算**：物品 Embedding 离线算好存 Redis
5. **模型优化**：INT8 量化、算子融合、GPU Batch 推理
6. **异步化**：日志/指标异步写，不阻塞主链路
7. **降级兜底**：各环节均有降级策略，最终兜底返回热门列表

---

### Q7：如何解决推荐系统的冷启动问题？

**答**：
**新用户冷启动**：
- 注册信息推断（渠道/设备/地域）
- 兴趣引导页（让用户选偏好标签）
- 前期增加 Explore 比例，展示多样化内容
- 逐步过渡到个性化

**新物品冷启动**：
- 内容理解：基于标题/封面 Embedding 找相似物品
- 流量扶持：保底曝光量 + 快速反馈
- Look-alike：复用相似物品的目标用户群

**Exploration 策略**：Thompson Sampling、UCB、ε-greedy，在利用已知和探索未知之间平衡

---

### Q8：特征系统如何设计？离线/近线/实时特征的区别？

**答**：

| 维度 | 离线特征 | 近线特征 | 实时特征 |
|------|---------|---------|---------|
| 更新周期 | T+1（天级） | 分钟级 | 请求级 |
| 计算方式 | Spark 批处理 | Flink 流计算 | 在线计算 |
| 存储 | HBase/Redis | Redis/Flink State | 内存 |
| 示例 | 用户画像、物品统计 | 实时 CTR、热度 | 上下文、交叉特征 |

特征服务架构：统一 Feature Store + 多级缓存（Local LRU → Redis → DB），降级时返回默认值。

---

> 📖 **总结**：推荐系统是"数据驱动 + 工程化"的典范。理解漏斗架构、掌握特征工程、熟悉 AB 实验是推荐工程师的核心能力。模型只是其中一环，工程架构（低延迟/高可用/可实验）同等重要。
>
> **推荐阅读**：*"深度学习推荐系统"* (王喆)、*"Recommender Systems: The Textbook"* (Aggarwal)

---

*最后更新：2026年3月*
